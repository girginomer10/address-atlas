## 2026-05-07 00:05 CEST - Codex

- Task: Started the native Mac rebuild with zero-knowledge encrypted sync.
- Changed: Added `native/AddressAtlasMac` SwiftUI/SwiftPM scaffold, core vault models, Keychain vault key manager, encrypted SQLite envelope store, native scanner/export/signing primitives, zero-knowledge passkey sync routes, sync envelope validation, docs, env examples, and package scripts.
- Verified: `npm test`, `npm run build`, `npx tsc --noEmit`, `swiftc -parse` over native Swift files, `git diff --check`, and conflict-marker scan.
- Not verified: `swift test` could not run in this workspace because only Command Line Tools are selected; SwiftPM fails while linking the manifest before compiling sources. Verify on a Mac with full Xcode selected.
- Memory: `docs/DEVELOPMENT.md` updated with native Mac and encrypted sync architecture notes.
- Next: Verify/fix Swift compile under full Xcode, expand native scanner token parity, wire real passkey UI in the Mac app, add SQL migrations for hosted sync deploys, and replace placeholder native exchange balance fetches with live REST clients after mocked signing coverage.
