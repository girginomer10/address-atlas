## 2026-07-12 16:00 +03 - Codex

- Task: Completed a repo-wide security, UX, logic, sync, scanner, release, and operations review, then remediated every actionable finding found in the primary and independent follow-up passes.
- Changed: hardened passkey/session handling, request limits, quotas, storage accounting, database health, and native config; upgraded the encrypted sync protocol and conflict handling; repaired vault recovery and persistence semantics; tightened scanner validation, exchange authentication/pagination, timeouts, partial-result behavior, and fiat valuation; added destructive-action confirmations, input preservation, accessibility labels, scan warnings, cancellation, and real auto-refresh; hardened universal macOS release/notarization scripts and production container configuration; upgraded dependencies and expanded regression coverage.
- Verified: `npm test` (44 passed, 1 Postgres integration test skipped without `TEST_SYNC_DATABASE_URL`); `npm run typecheck`; `npm run build`; production and full `npm audit` (0 vulnerabilities); `npm run native:test` (73 passed, 2 credential-gated live exchange tests skipped); universal `arm64` + `x86_64` hardened-runtime app build and signature inspection; actual macOS UI, validation, locked-vault recovery, and cleanup flow; shell syntax, Compose YAML parsing, and `git diff --check`.
- Memory: no repo semantic-memory system is installed. Durable protocol and release decisions are summarized here: sync v2 authenticates account/version/schema as associated data; new v1 uploads are blocked while stored v1 snapshots remain migration-readable; native config is v3 with app version 0.2.0; credential-bearing exchange origins/routes are binary-pinned; Coinbase uses CDP ES256 JWT; CoinGecko's BTC-relative exchange rates provide supported fiat-to-USD valuation.
- Next: deploy the 0.2 native app and sync server as a coordinated release; existing sessions must sign in again, and legacy Coinbase HMAC credentials must be re-entered as a CDP key name plus ES256 private key. When the required external environment is available, run the credential-gated Postgres, Docker/Caddy, live-exchange, Developer ID, and notarization checks.
