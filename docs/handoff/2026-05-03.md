## 2026-05-03 15:23 CEST - Codex

- Task: Expanded popular token coverage and upgraded custom token handling.
- Changed: Added broader built-in EVM/Solana token registries, Token-2022 SPL scanning, Solana custom mint support, optional CoinGecko ids, manual USD fallback prices, `/api/tokens/metadata`, and Settings UI autofill/manual-price controls.
- Verified: `npm test` (13 files / 70 tests), `npm run build`, `npm run db:push`, `git diff --check`, conflict-marker scan, and runtime smoke checks for `/settings`, `/api/tokens`, and `/api/tokens/metadata` on `http://localhost:3000`.
- Memory: `docs/DEVELOPMENT.md` updated with custom token pricing/metadata and Token-2022 behavior.
- Next: Add CoinGecko id suggestions after metadata lookup and optional Solana metadata-program/DAS symbol/name autofill.

## 2026-05-03 15:27 CEST - Codex

- Task: Audited the token expansion for weak spots after integration.
- Changed: Made Solana SPL scanning tolerate partial RPC failure between classic SPL Token Program and Token-2022 lookups, so one failing program no longer hides balances from the other; added regression coverage.
- Verified: `npm test` (13 files / 71 tests), `npm run build`, and live CoinGecko simple-price check for all 34 built-in registry ids.
- Memory: none; this is a scanner robustness fix covered by tests.
- Next: Consider adding CoinGecko id suggestions and Solana metadata-program/DAS symbol/name autofill.

## 2026-05-03 15:34 CEST - Codex

- Task: Continued the token-polish pass beyond the basic expansion.
- Changed: `/api/tokens/metadata` now uses registry-first metadata, returns CoinGecko id suggestions, avoids unnecessary RPC calls for built-ins, fixes Solana mint decimals lookup to use `getAccountInfo`, and can use `JUPITER_API_KEY` for arbitrary Solana symbol/name/USD price hints; Settings shows suggestion chips for CoinGecko ids.
- Verified: `npm test` (13 files / 72 tests), `npm run build`, and runtime smoke checks for `/settings`, Ethereum USDC metadata, and Solana BONK metadata on `http://localhost:3000`.
- Memory: `docs/DEVELOPMENT.md` updated with metadata suggestion and optional Jupiter API key behavior.
- Next: If we want no-key Solana symbol/name for every arbitrary mint, add a Metaplex metadata-program or DAS lookup fallback.
