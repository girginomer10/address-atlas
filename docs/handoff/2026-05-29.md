## 2026-05-29 00:39 +03 - Claude

- Task: Shipped remaining deep-review fixes (P2 + cross-domain) across the web app, then removed the web app entirely so the macOS native app is the only client and the repo root is an encrypted sync-only server.
- Changed (in order, each its own commit, all pushed to origin/main):
  - `6418fb4` Phase A backend: exchange NaN guard, failed-snapshot error read-back, guarded JSON.parse, createdAt tiebreak, scanTron try/catch, manual price/value reconcile.
  - `5154c56` Phase B: portfolio total made server-authoritative (`scan.summary.totalUsd`); addressCount from `run.inputCount`; dust filter now affects only list rows.
  - `c331559` Phase C frontend: render `scan.warnings`, guard date helpers, savePrefs rollback, scan-response sequencing by `generatedAt`, CSV formula-injection guard.
  - `e1fb698` Phase D web sync security: single-use WebAuthn challenges (`consumed_challenges` table), proxy matcher covers dotted paths, transactional passkey registration.
  - `1b91dd1` Native E1/E2: EVM scan error isolation (mirrors Solana), https required for chain/price endpoint config (localhost exception).
  - `99364ce` Removed the web app: deleted the Next.js portfolio UI + `src/app/api/*` + scanner/chain-registry/exchange/local-store libs + Prisma/SQLite + web build scripts; trimmed deps (prisma, better-sqlite3, ccxt, lucide-react, protobufjs); updated package.json/next.config/.env.example/README. What remains at repo root is the sync-only server (auth/passkey, auth/native, config/native, healthz, vault/latest + `src/lib/sync/*` + `server/sync` + Postgres).
- Verified: `npm run typecheck` clean; `npm test` (vitest, now 4 passed / 1 skipped — only sync tests remain, no Prisma); `npm run build` produces only the 6 sync routes + proxy middleware; `npm run native:test` (37 passed / 2 skipped) before the native commit.
- Memory: removing the auth dev-fallback (earlier P1) means the sync server requires `SYNC_SESSION_SECRET` in every env. The server is sync-only via `ADDRESS_ATLAS_SYNC_ONLY=true` + `src/proxy.ts`. `to_fix.md` (full P1/P2 finding list) is kept local-only and untracked since this is a public repo.
- Next (deferred follow-ups, still in local `to_fix.md`): D3 — native token still delivered via `address-atlas://` query string; one-time-code exchange not yet implemented (needs coordinated web+native change + manual passkey E2E). E3 — `minSupportedAppVersion` decoded but not enforced in the native client. Lower-severity items remain noted in `to_fix.md`.
