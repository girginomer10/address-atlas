## 2026-06-21 15:45 +03 - Claude

- Task: Ran a deep reachability audit (6 parallel agents) over the current codebase (sync-only server + native macOS app), recorded findings, and fixed the two P1 (critical) items.
- Changed:
  - `server/sync/Dockerfile` — removed the runner-stage `COPY /app/prisma` and `COPY /app/src/generated` lines. Both paths no longer exist after the web-app removal (`prisma/` deleted; `src/generated` is `.dockerignore`d), so the prod image could not build at all. Runner now copies only `package.json`, `node_modules`, `.next`.
  - `src/app/vault/latest/route.ts` — hardened the vault UPSERT conflict clause. The same-version idempotent branch gated only on the client-supplied top-level `checksum`, which the server never recomputes, so a token holder could replay the old checksum with swapped ciphertext and overwrite the vault at an unchanged version. The branch now additionally requires `envelope->>'checksum'` (the inner checksum, verified against the ciphertext by `assertEnvelopeChecksum`) to match the stored row, making a same-version content swap require a SHA-256 preimage.
- Verified: `npm run typecheck` clean; `npm test` (vitest) 8 passed / 1 skipped; `npm run build` succeeds and emits exactly the 6 sync routes + Proxy middleware (no Prisma). Docker build itself NOT run (no daemon in this env) — Dockerfile fix verified by static analysis + the builder-stage build passing.
- Memory: full finding list (2 P1 done, 11 P2, ~12 risk/low, test gaps) is in local-only untracked `to_fix.md`. The 2026-05-28 web-app review is superseded — those files (scanner.ts, components/, prisma/) no longer exist. Verified-fixed items from prior commits are catalogued at the top of `to_fix.md` so they aren't re-chased.
- Next (highest-value remaining, all P2): (1) require https + add cert pinning on the native sync server base URL — `AppState.swift` only trims it (P2-1); (2) guard the native download path against server-version rollback — `downloadEncryptedVault` trusts `snapshot.version` unconditionally (P2-2); (3) body-size cap on `PUT /vault/latest` + prune `consumed_challenges` + rate-limit auth endpoints (P2-4/5/6); (4) non-root container `USER` + HSTS at Caddy (P2-7/8); (5) SPL decimals-mismatch warning + Coinbase Advanced-Trade signing scheme (P2-9/10).

## 2026-06-21 19:50 +03 - Claude

- Task: Per `/goal` ("fix everything found"), resolved ALL remaining P2 + risk findings from the deep review (the P1s were already done earlier today).
- Changed (sync server / TS): `passkeys.ts` (consume challenge after WebAuthn verifies + inside reg txn; orphan-user fork fixed via RETURNING user_id; unify credential-enumeration error; opportunistic `consumed_challenges` prune), `postgres.ts` (prune index), `envelope.ts` (strict key-allowlist replaces plaintext substring scan; `MAX_ENVELOPE_BYTES` + `MAX_VERSION` caps), `vault/latest/route.ts` (413 content-length guard), `sync-only.ts` (fail-closed gate), `rate-limit.ts` (new; 20/min/IP on both auth routes), `native-config.ts` (emit semver-validated `minSupportedAppVersion`), `next.config.mjs` (security headers), `layout.tsx` (sync-server metadata + noindex).
- Changed (infra): `server/sync/Dockerfile` (`USER node` + `--chown`), `server/sync/Caddyfile` (HSTS + security headers + `request_body max_size 9MB`).
- Changed (native Swift): `AppState.swift` (https-only `validatedSyncURL` everywhere; download version-rollback guard; `minSupportedAppVersion` enforcement via `CFBundleShortVersionString`), `PasskeyWebAuthenticator.swift` + `NativePasskeyBridge.tsx` + `page.tsx` (state-nonce CSRF binding + host validation on the callback), `NativeScanner.swift` (SPL on-chain decimals + warning; NaN guards), `ExchangeClients.swift`/`ExchangeRequestSigning.swift` (non-redirecting session; Coinbase secret base64-decoded), `HTTPClient.swift` (non-redirecting session + timeouts), `Exporters.swift` (CSV formula-injection guard), `AddressDetection.swift` (chain-aware dedup), `NativeEndpointConfig.swift` (loopback `::1`; timeout), request timeouts across sync/config/exchange/chain/price.
- Verified: `npm run typecheck` clean; `npm test` 8 passed/1 skipped (updated one envelope assertion for the new key-allowlist behavior); `npm run build` clean (6 routes + proxy); `swift build` clean. `swift test` NOT runnable here (no Xcode/XCTest — CommandLineTools only); native changes are build-verified and statically checked vs existing fixtures (Solana tests don't hit the new decimals branch; Coinbase live tests are XCTSkip-gated; touched public parsers unchanged).
- Memory: accepted-by-design residuals (documented in `to_fix.md`, not changed): Keychain biometric gate (device-only is correct for a software vault), local decrypt error distinction (no remote oracle), Cosmos 500-delegation cap (unreachable), cert pinning (needs operator cert), token-in-callback-URL (in-process via ASWebAuthenticationSession; one-time-code exchange is a larger follow-up), Coinbase JWT/ES256 (needs live validation). Test additions deferred per the standing "no tests unless asked" rule.
- Next: if desired — Coinbase Advanced Trade JWT auth (live validation needed), cert pinning, one-time-code token exchange, and the deferred test suite.

## 2026-06-22 00:10 +03 - Claude

- Task: Ran the native XCTest suite to close the earlier "could not run swift test here" gap (user installed Xcode).
- Verified: `npm run native:test` → **37 passed / 2 skipped / 0 failures** (Apple Swift 6.3.2 via Xcode). The 2 skips are the env-gated live-exchange smoke tests (`ADDRESS_ATLAS_LIVE_EXCHANGE_TESTS=1`). Matches the pre-change baseline, confirming the 2026-06-21 hardening broke no tests. All changed-path suites green (NativeScannerToken, NativeExchangeClient, SyncClient, VaultCrypto, RecoveryKit).
- Memory: native side is now build- AND test-verified (no longer just build-verified). No code changes this entry.
- Next: unchanged — Coinbase Advanced Trade JWT (needs a live key), cert pinning, one-time-code token exchange remain the open follow-ups.
