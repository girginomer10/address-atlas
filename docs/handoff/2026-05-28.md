## 2026-05-28 23:25 +03 - Claude

- Task: Fixed the 5 critical (P1) findings from a multi-agent deep review (runtime wiring & reachability audit) covering sync auth, vault integrity, scan persistence, and the Solana scanner.
- Changed:
  - `src/lib/sync/tokens.ts`: `secret()` now throws when `SYNC_SESSION_SECRET` is unset in every environment (removed the hardcoded dev fallback); `verifyToken` rejects tokens that lack a numeric `expiresAt`.
  - `src/lib/sync/envelope.ts` + `src/app/vault/latest/route.ts`: added `assertEnvelopeChecksum` (SHA-256 over `schema|crypto|key|` + raw nonce + raw ciphertext) and call it on PUT, so a same-version garbage-ciphertext overwrite is rejected. Updated `vault/latest/route.test.ts` fixture to a valid checksum.
  - `prisma/schema.prisma` + `src/lib/local-store.ts`: added nullable `exchangeId`/`exchangeProvider` columns to `Holding`; `holdingData` writes them and `holdingToAsset` restores them, so exchange holdings keep attribution across reloads.
  - `src/lib/scanner.ts`: `fetchSolanaSplBalances` no longer skips on a registry/on-chain decimals mismatch; it aggregates with the authoritative on-chain decimals and warns on divergence.
- Verified: `npm run typecheck` (clean) and `npm test` (92 passed, 1 skipped — the Postgres integration test needs a live DB). `npm run db:push` applied the new nullable columns to the local SQLite DB. WBTC mint `9n4nbM75…` on-chain decimals confirmed = 6 via Solana RPC, so the registry value was already correct (no change needed).
- Memory: durable scanner/sync behavior already documented in `docs/DEVELOPMENT.md`; no separate semantic-memory directory installed. Note: removing the auth fallback means local sync/auth/vault routes now require `SYNC_SESSION_SECRET` in `.env` (already in `.env.example`); the main SQLite web app is unaffected.
- Next: P2 + lower-severity findings remain (frontend total vs server-summary drift, swallowed `scan.warnings`, exchange `valueUsd` NaN guard, unguarded JSON.parse in `latestScanResponse`, WebAuthn challenge replay, CSV formula injection, etc.). They are tracked in a local-only `to_fix.md` at repo root (intentionally NOT committed, since this is a public repo and the file details unpatched issues). Decide disclosure/fix order before publishing that list.
