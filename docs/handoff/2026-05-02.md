## 2026-05-02 16:05 CEST - Codex

- Task: Implemented the local-first full product plan based on design v4.
- Changed: Added Prisma/SQLite persistence, encrypted vault helpers, wallet/preferences/exchange API routes, ccxt Binance/Coinbase/Kraken balance adapters, route-based app UI, archive-style responsive CSS, export helpers, tests, and docs.
- Verified: `npm test`, `npm run build`, `/api/scan` smoke test with a public Solana address, and Playwright-core viewport overflow checks for `/`, `/wallets`, `/assets`, `/snapshots`, `/export`, and `/settings` at 1440px, 430px, and 390px.
- Memory: `docs/DEVELOPMENT.md` updated with local-first architecture and security constraints.
- Next: Add SPL token balances, Cosmos staking/rewards, historical scan charts, and optional manual CEX fallback snapshots.

## 2026-05-02 17:56 CEST - Codex

- Task: Fixed local data safety issues from the deep review.
- Changed: Isolated test DB setup, added fresh SQLite bootstrap, made scan persistence transactional, filtered stale deleted-wallet holdings from latest snapshots, rejected unsupported public strings before wallet save, and added CoinGecko timeout/fallback handling.
- Verified: `npm test`, `npm run build`, and `DATABASE_URL="file:./.data/review-fresh.db" npm run db:push`.
- Memory: none; changes are covered by tests and this handoff.
- Next: Consider showing skipped unsupported addresses more prominently in the UI warning area.

## 2026-05-02 22:07 CEST - Codex

- Task: Debugged the portfolio allocation donut visual issue reported from the browser.
- Changed: Updated `.aa-donut` layout from grid centering to a flex column so the chain count and label stay grouped in the center of the donut hole.
- Verified: In-app browser visual check, `npm test`, `npm run build`, and Playwright-core overflow checks for `/`, `/wallets`, `/assets`, `/snapshots`, `/export`, and `/settings` at 390px, 430px, and 1440px.
- Memory: none; this is a CSS layout fix with no durable architecture decision.
- Next: Keep an eye on full-page screenshot stitching with the sticky mobile bar; the live viewport looked correct.

## 2026-05-02 23:05 CEST - Claude

- Task: Added SPL token balance support to Solana scanning (USDC and USDT on mainnet).
- Changed:
  - `src/lib/types.ts`: introduced `SplTokenConfig`; widened `AssetSource` to include `"spl"`.
  - `src/lib/chain-registry.ts`: added `SOLANA_TOKEN_PROGRAM_ID`, `SPL_TOKENS_BY_CHAIN` registry (USDC, USDT), and included SPL CoinGecko ids in `getAllCoinGeckoIds()`.
  - `src/lib/scanner.ts`: extended `scanSolana` to fetch SPL token balances via `getTokenAccountsByOwner` (jsonParsed, classic Token program), exported a pure `parseSplTokenAccounts` helper, sums multi-account holdings per mint, validates registry decimals, and emits assets with `source: "spl"`.
  - `src/lib/scanner.test.ts`: new focused tests for the SPL parser and a fetch-mocked end-to-end scan that validates SOL + USDC + USDT and an unknown-mint filter, plus a warning-on-RPC-failure path.
- Commands run:
  - `DATABASE_URL=file:./.data/address-atlas.test.db npx prisma db push` (non-destructive, applied schema to the empty test DB so Prisma-backed tests could run without `--force-reset`).
  - `DATABASE_URL=file:./.data/address-atlas.test.db NODE_ENV=test npx vitest run` -> 17/17 tests pass.
  - `npx tsc --noEmit` -> clean (no type errors).
  - `npm run build` -> failed in this environment with a pre-existing Turbopack symlink panic ("Symlink [project]/node_modules is invalid, it points out of the filesystem root"); the workspace ships `node_modules` as a symlink to a sibling directory. Not caused by this change.
- Risks:
  - Token-2022 (program `TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb`) accounts are not queried; only classic SPL token program holdings are scanned. Tokens minted under Token-2022 will not appear.
  - Registry decimals must match on-chain; mismatched accounts are skipped defensively, which would silently zero out a mint if its decimals are ever changed in the registry by mistake.
  - The default public Solana RPC (`api.mainnet-beta.solana.com`) is rate-limited; heavy parallel scans may surface transient warnings.
- Next:
  - Add Token-2022 support (second `getTokenAccountsByOwner` call with the Token-2022 program id) once we want native USDC-2022 or BONK22 coverage.
  - Optionally surface SPL fetch warnings in the address-detail UI alongside existing native warnings.
  - Verify `npm run build` end-to-end on a checkout with a real `node_modules` directory (not a cross-filesystem symlink).

## 2026-05-02 23:13 CEST - Claude/Codex

- Task: Added Cosmos staking and rewards balances on the isolated `codex/claude-cosmos-rewards` branch.
- Changed: `src/lib/scanner.ts` now fetches native liquid balances, delegations, and distribution rewards from Cosmos REST endpoints, emits separate `native`, `staked`, and `rewards` assets, and warns instead of failing when staking/rewards endpoints are unavailable; `src/lib/types.ts` extends `AssetSource`; `src/lib/scanner.test.ts` adds parser and scan integration coverage.
- Verified: `npx vitest run src/lib/scanner.test.ts` and `npx tsc --noEmit`.
- Memory: none; behavior is covered by focused tests and this handoff.
- Next: Run the full repo test/build after integrating branches; review whether UI labels should visually distinguish staked/rewards rows beyond the asset name.

## 2026-05-02 23:08 CEST - Claude

- Task: Added an editable token allowlist so users can extend ERC-20 scanning beyond the built-in registry.
- Changed:
  - `prisma/schema.prisma`: new `CustomToken` model (chainKind/chainId/address compound unique, decimals 0-36, enabled flag, generic enough for future SPL).
  - `src/lib/local-store.ts`: `listCustomTokens` / `listEnabledCustomTokens` / `createCustomToken` / `updateCustomToken` / `deleteCustomToken`, plus `normalizeCustomTokenInput` validation and a `CustomTokenValidationError` class with field hints.
  - `src/lib/test-db.ts`: clears the new table between test runs.
  - `src/lib/scanner.ts`: pulls enabled custom EVM tokens, dedupes against built-ins (built-in wins), feeds the merged set into `scanEvm`, and includes custom CoinGecko ids in the price fetch so values aren't $0.
  - `src/app/api/tokens/route.ts`: GET / POST / PATCH / DELETE handlers with structured 400s.
  - `src/components/AddressAtlasApp.tsx` + `src/app/globals.css`: a new "Token allowlist" block on Settings with a chain dropdown, add form, enable toggle, and delete action; styles match the existing setting-block / form patterns.
  - `src/lib/token-allowlist.test.ts`, `src/app/api/tokens/route.test.ts`: focused persistence, validation, scanner-merge, and HTTP-handler tests.
- Verified:
  - `npx tsc --noEmit` clean.
  - `DATABASE_URL=file:./.data/address-atlas.test.db npx prisma db push` then `npx vitest run` -> 28 / 28 tests pass (8 test files, including 2 new).
- Risks / known limitations:
  - `npm run build` fails before reaching my code with `TurbopackInternalError: Symlink [project]/node_modules is invalid, it points out of the filesystem root`. Reproduces on `git stash` of these changes, so it's a pre-existing workspace issue (this repo's `node_modules` is symlinked to the sibling `address-atlas/node_modules`). `npm run dev` fails the same way, so I could not browser-verify the new Settings block; type-checking and unit tests are the only validation. The fix is to give this clone its own `node_modules` (e.g. `npm install` directly here, or replace the symlink).
  - `npm test` couldn't run end-to-end because `scripts/run-tests.mjs` invokes `prisma db push --force-reset`, which Prisma 7 blocks for AI agents without `PRISMA_USER_CONSENT_FOR_DANGEROUS_AI_ACTION`. I substituted an additive `prisma db push` + `vitest run`. The test script is unchanged.
  - `loadCustomEvmTokens` in `scanner.ts` swallows DB errors so a stale schema can't break a scan; downside is missing migrations could silently disable the allowlist. Acceptable while the schema/code ship together.
- Next: Consider letting the form auto-fill `decimals`/`coinGeckoId` from a contract-call lookup; expose enabled-token count somewhere on Settings; revisit the non-destructive db push in CI/agent contexts.

## 2026-05-02 23:30 CEST - Codex

- Task: Reviewed and integrated the useful Claude worktree outputs into the main workspace.
- Changed: Merged Solana SPL tokens, Cosmos staking/rewards, scan history, custom token allowlist, manual exchange entries, display-currency FX formatting, and auto-refresh into the main app/API/schema/UI; resolved cross-branch conflicts in scanner, local store, Prisma schema, and `AddressAtlasApp`; fixed an FX retry state-loop risk found during review.
- Verified: `npm test` (12 files / 66 tests), `npm run build`, `npm run db:push`, `git diff --check`, conflict-marker scan, and runtime smoke checks for `/`, `/snapshots`, `/api/scan/history`, `/api/fx`, `/api/tokens`, and `/api/exchanges/manual` on `http://localhost:3000`.
- Memory: `docs/DEVELOPMENT.md` updated with completed feature notes and the Prisma dev-server restart gotcha.
- Next: Token-2022 support, token metadata autofill, and a product decision on whether manual holdings should ever become time-specific historical snapshot entries.
