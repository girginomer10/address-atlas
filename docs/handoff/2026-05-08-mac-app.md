## 2026-05-08 00:12 CEST - Codex

- Task: Continued toward a full working native Mac app surface.
- Changed: Reworked the SwiftUI app into a fuller sidebar app with Portfolio, Wallets, Assets, Tokens, Snapshots, Exchanges, Sync, Export, and Settings; added AppState actions for add/remove/toggle custom tokens, manual holdings, wallets, encrypted exchange credentials, and encrypted vault upload/download; added sync settings to the encrypted vault; added native `.app` bundle build script and native README.
- Verified: `swiftc -parse` over native Swift files, `npx tsc --noEmit`, `npm test`, `npm run build`, `git diff --check`, and conflict-marker scan.
- Not verified: `swift test` and `./build-mac-app.sh` still require full Xcode; this workspace only has Command Line Tools and SwiftPM fails while linking the package manifest.
- Memory: `docs/DEVELOPMENT.md` updated with the native app bundle script note.
- Next: Run native compile/tests on full Xcode, fix any type issues that parse-only cannot catch, replace manual session-token sync with native AuthenticationServices passkey registration/login, and add live exchange balance clients.

## 2026-05-08 00:25 CEST - Codex

- Task: Filled in native exchange scanning and hardened the Mac build preflight.
- Changed: Added Swift REST balance clients for Binance, Coinbase, and Kraken; added exchange balance normalization/pricing; wired encrypted exchange connections into the native snapshot scan; expanded exchange UI status/actions; added mocked native exchange tests; added `check-toolchain.sh` and made the app-bundle script fail early when full Xcode is not selected.
- Verified: `swiftc -parse` over native Swift files, `npx tsc --noEmit`, `npm test`, `npm run build`, `git diff --check`, and `./check-toolchain.sh` preflight behavior.
- Not verified: `swift test` still cannot compile the package manifest on this machine because `xcode-select` points to `/Library/Developer/CommandLineTools`; full Xcode is required.
- Memory: `docs/DEVELOPMENT.md` updated with the native exchange client note.
- Next: Verify native type-check/tests on a Mac with full Xcode selected, then replace manual sync token entry with native AuthenticationServices passkey registration/login.

## 2026-05-08 00:34 CEST - Codex

- Task: Rounded off native Mac app day-to-day controls.
- Changed: Added macOS save dialogs for CSV/JSON export, editable wallet labels, and snapshot deletion from the SwiftUI app.
- Verified: `swiftc -parse` over native Swift files and `git diff --check`.
- Not verified: Full native type-check/tests remain blocked until full Xcode is selected instead of Command Line Tools.
- Memory: none; no new durable architecture decision.
- Next: Full Xcode verification, then native passkey auth flow.

## 2026-05-08 00:45 CEST - Codex

- Task: Added a usable passkey bridge for encrypted sync.
- Changed: Added `/auth/native` web bridge using `@simplewebauthn/browser`, wired the Mac app to open it with `ASWebAuthenticationSession`, registered `address-atlas://sync-auth` in the app bundle, and saved returned sync account/session state into the encrypted vault.
- Verified: `swiftc -parse` over native Swift files, `npx tsc --noEmit`, `npm test`, `npm run build`, and `git diff --check`.
- Not verified: Full native type-check/tests remain blocked until full Xcode is selected instead of Command Line Tools.
- Memory: `docs/DEVELOPMENT.md` updated with the native passkey bridge contract.
- Next: Verify the full Mac app bundle on full Xcode and a configured HTTPS/passkey origin; then exercise register, upload, restart, sign-in, download.

## 2026-05-08 00:50 CEST - Codex

- Task: Made local encrypted sync easier to run.
- Changed: Added `compose.sync.yml` for a dev Postgres sync database and npm scripts `sync:db:up` / `sync:db:down`.
- Verified: not run; compose service was not started in this session.
- Memory: `docs/DEVELOPMENT.md` notes the local sync Postgres helper.
- Next: Start compose locally, run Next with `.env.example` sync values, and use the Mac Sync page passkey flow.

## 2026-05-08 01:00 CEST - Codex

- Task: Installed local native build prerequisites and replaced the rough Mac UI with the real Address Atlas design language.
- Changed: Installed Homebrew `mas`, `xcodes`, and Swift 6.2.4; Xcode 26.4.1 is present but its license was not accepted in the admin prompt; started the encrypted sync Postgres container on port `15432`; rebuilt the SwiftUI app with a custom editorial sidebar, branded unlock screen, dense portfolio dashboard, border-based panels, table-like asset rows, and redesigned Wallets/Tokens/Snapshots/Exchanges/Sync/Export/Settings screens; updated build scripts for the Homebrew Swift fallback; ignored SwiftPM `.build`.
- Verified: `DEVELOPER_DIR=/Library/Developer/CommandLineTools PATH="/opt/homebrew/opt/swift/bin:$PATH" swift build --product AddressAtlasMac`, `./build-mac-app.sh`, launched `native/AddressAtlasMac/dist/Address Atlas.app`, `npx tsc --noEmit`, `npm test`, `npm run build`, `git diff --check`, and Docker health for `address-atlas-sync-postgres`.
- Not verified: `swift test` still needs full Xcode/XCTest and the Xcode license prompt was cancelled by the user.
- Memory: `docs/DEVELOPMENT.md` updated with the Homebrew Swift fallback note.
- Next: Accept the Xcode license with admin credentials when available, then run `swift test` under full Xcode; visually review the live app and tune spacing/copy if needed.

## 2026-05-08 01:08 CEST - Codex

- Task: Continued native scanner parity for the Mac app.
- Changed: Added TRON TRC20 registry support with USDT, Cosmos delegation/reward parsing, XRP issued-currency trustline parsing, scanner parser tests, and docs scope updates.
- Verified: `DEVELOPER_DIR=/Library/Developer/CommandLineTools PATH="/opt/homebrew/opt/swift/bin:$PATH" swift build --product AddressAtlasMac`, `./build-mac-app.sh`, `npx tsc --noEmit`, `npm test`, and `npm run build`.
- Not verified: `swift test` still fails in this workspace because the fallback Swift toolchain does not provide XCTest; full Xcode license acceptance is still needed.
- Memory: `docs/DEVELOPMENT.md` updated to mark TRC20/Cosmos staking-reward scanner work as present and keep remaining scanner milestones scoped.
- Next: Add broader built-in TRC20/issued-asset pricing maps, surface per-chain partial-scan warnings, then run `swift test` once full Xcode is licensed.

## 2026-05-08 16:13 CEST - Codex

- Task: Brought native token registries closer to the web reference.
- Changed: Expanded native ERC-20 registries for Ethereum, Base, Arbitrum, Optimism, Polygon, BSC, and Avalanche; fixed native Solana USDT/BONK mint addresses; added WBTC, JitoSOL, and WIF SPL entries; added registry coverage tests and docs updates.
- Verified: `DEVELOPER_DIR=/Library/Developer/CommandLineTools PATH="/opt/homebrew/opt/swift/bin:$PATH" swift build --product AddressAtlasMac`, `./build-mac-app.sh`, `npx tsc --noEmit` after Prisma generation, `npm test`, `npm run build`, and `git diff --check`.
- Not verified: `swift test` remains blocked until full Xcode/XCTest is available after license acceptance.
- Memory: `docs/DEVELOPMENT.md` notes that the native ERC-20/SPL registry mirrors the web reference's common token set.
- Next: Surface partial scan warnings for token/staking subrequests and add XRPL issued-asset pricing hints.

## 2026-05-08 16:20 CEST - Codex

- Task: Surfaced partial native scan failures instead of silently dropping optional data.
- Changed: Native chain scans now carry assets plus warnings; ERC-20 token probe failures, Solana SPL/Token-2022 account request failures, Cosmos delegation/reward failures, and XRP trustline failures keep successful native balances while recording user-visible warnings. Added an async Solana scanner warning test and docs updates.
- Verified: `DEVELOPER_DIR=/Library/Developer/CommandLineTools PATH="/opt/homebrew/opt/swift/bin:$PATH" swift build --product AddressAtlasMac`, `./build-mac-app.sh`, `npx tsc --noEmit`, `npm test`, `npm run build`, and `git diff --check`.
- Not verified: `swift test` still fails before running tests because the fallback toolchain cannot import XCTest.
- Memory: `docs/DEVELOPMENT.md` updated to document partial-scan warning behavior.
- Next: Add XRPL issued-asset pricing hints and continue token metadata autofill parity.

## 2026-05-08 16:27 CEST - Codex

- Task: Re-verified native builds after full Xcode license acceptance.
- Changed: Made native build scripts prefer full Xcode over the Homebrew Swift fallback when Xcode is selected, added local ad-hoc signing plus codesign verification to the `.app` bundle build, and updated docs to reflect the new Xcode/toolchain state.
- Verified: `xcodebuild -license check`, `xcodebuild -version`, `swift test` (17 passed), `./check-toolchain.sh`, `./build-mac-app.sh` with codesign verification, `npx tsc --noEmit`, `npm test`, `npm run build`, `git diff --check`, and launch smoke via `open -n native/AddressAtlasMac/dist/Address Atlas.app`.
- Memory: `docs/DEVELOPMENT.md` updated to record that full Xcode 26.4.1 is selected/licensed and native XCTest now runs.
- Next: Add Developer ID signing/notarization for distribution, then run passkey sync E2E against a real HTTPS origin.

## 2026-05-08 16:30 CEST - Codex

- Task: Cleaned up native Mac UI copy and contrast.
- Changed: Removed the persistent "Vault unlocked from macOS Keychain" notice after auto-unlock, replaced repeated Keychain sidebar copy with simpler local encryption copy, forced the SwiftUI app into the light theme used by the design, and pinned custom text field/surface foreground colors to avoid white text on light panels.
- Verified: `swift build --product AddressAtlasMac`, `swift test` (17 passed), `./build-mac-app.sh` with codesign verification, `rg "Vault unlocked|Keychain unlocked|macOS Keychain" native/AddressAtlasMac/Sources/AddressAtlasMac`, `git diff --check`, and launch smoke via `open -n native/AddressAtlasMac/dist/Address Atlas.app`.
- Memory: none; UI polish only.
- Next: Visual pass on the opened app to catch any remaining system-control contrast issues.

## 2026-05-08 17:04 CEST - Codex

- Task: Implemented public native macOS v1 readiness scaffolding.
- Changed: Added VPS production sync-only Docker/Caddy stack, health check, sync route guard via Next `proxy.ts`, recovery kit crypto and Settings UI, hidden sync session token handling, expired-session errors, DMG/notarization scripts, release checklist, opt-in live exchange smoke tests, exchange HTTP error messages, and encrypted Postgres snapshot leak checks.
- Verified: `swift test`, `./build-dmg.sh`, `npx tsc --noEmit`, `npm test`, `npm run build`, `docker compose --env-file .env.production.example -f compose.prod.yml config`, `git diff --check`, and `security find-identity -v -p codesigning`.
- Memory: Durable release gates are documented in `docs/RELEASE_CHECKLIST.md`; no separate semantic memory directory exists.
- Next: Developer ID Application certificate is still missing, so public release remains blocked until signing/notarization can run. Then run VPS passkey/sync E2E and live Binance/Coinbase/Kraken smoke tests with out-of-band read-only credentials.

## 2026-05-09 00:47 CEST - Codex

- Task: Split the production sync server deployment and added remote endpoint config.
- Changed: Moved production Caddy/Docker/Compose/env files under `server/sync`, added `/config/native` with env/JSON-driven RPC-price-exchange endpoint config, allowed it in sync-only mode, and wired the Mac app to refresh and use that config for native scans, prices, and exchange clients.
- Verified: `swift test`, `npx tsc --noEmit`, `npm test`, `npm run build`, `docker compose --env-file server/sync/.env.production.example -f server/sync/compose.prod.yml config`, `./build-dmg.sh`, and `git diff --check`.
- Memory: `server/sync/README.md` documents the server deployment target and endpoint-config contract.
- Next: On the real VPS, set `server/sync/.env.production`, verify `/config/native`, then test changing `NATIVE_ENDPOINT_CONFIG_JSON` and confirming the Mac app picks it up before scan.
