## 2026-09-25 01:35 +03 - Claude

- Task: Add a native iPhone/iPad app on the shared Swift core and state layer ("ios app yaz"). Work was stopped on the user's instruction after the first review pass; this entry records exactly what is done and what is not.
- Changed:
  - `native/AddressAtlasMac/Package.swift` declares `.iOS(.v17)`; no new package targets.
  - Shared-layer platform leaves (macOS behavior byte-identical, 500 Mac tests green): `PlatformCopy` nouns in AddressAtlasCore and in the AppState/iCloud/passkey/error strings; UIKit shims in `AtlasDesignSystem.swift` (dynamic colors, announcer priority, Page width); `AtlasFormatting.swift` and `ExportPipeline.swift` (with `ExportCopy`) extracted view-free; `ICloudVaultService.isConfigured` iOS branch reads the executable's `__TEXT,__entitlements` section (via `dladdr`) or `embedded.mobileprovision`; `PasskeyWebAuthenticator` gets a UIWindow anchor; `KeychainVaultKeyStore` forces the macOS legacy-item migration off on iOS.
  - New `native/AddressAtlasiOS`: xcodegen `project.yml` plus the committed `.xcodeproj`, `Sources/AddressAtlasiOS` (app, `RootView` lifecycle, iPhone tab shell and iPad split shell, ten screens), `Resources` (Info.plist, entitlements with the shared iCloud container, Production environment, and keychain groups; opaque 1024 icon rendered from `AppIcon-iOS.svg`; launch/accent colors), `scripts/write-version-xcconfig.sh`, `generate-project.sh`, `build-ios-app.sh`, `Version.xcconfig` (+ git-ignored `Version.generated.xcconfig`), README.
  - CI job `native-ios` (committed-project drift check, Core-for-iOS strict build, simulator build, bundle validation); npm scripts `native:ios:*`; `.gitignore` for Xcode state, `.swiftpm/`, agent worktrees.
  - Docs: README, DEVELOPMENT, RELEASE_CHECKLIST, ICLOUD, native READMEs, PRIVACY/TERMS/SUPPORT wording, CONTRIBUTING, SECURITY, app-store packet iOS sections.
- Verified (completed):
  - Mac: `swift test` 500 tests / 3 skipped / 0 failures; `swift test --sanitize=thread` 500 / 4 skipped / 0 failures; strict-concurrency warnings-as-errors build; `Tests/build-mac-app-version-tests.sh`, `notarize-mac-app-tests.sh`, `mas-release-contract-tests.sh`, `icloud-entitlements-tests.py` all pass; `scripts/check-secret-artifacts.py` clean (429 files).
  - iOS: `xcodebuild` simulator build with `SWIFT_STRICT_CONCURRENCY=complete` and warnings-as-errors, zero warnings; Core built for the iOS simulator triple under the same flags.
  - iPhone 17 Pro simulator, by hand: Keychain key creation, encrypted SQLite vault with strict durability pragmas, tab shell, Wallets add (address detection), a real scan (71 assets valued, snapshot persisted), Assets list, Exchanges form, More list navigation, Snapshots, iCloud (fail-closed "unavailable" message, no crash), Export preview through the shared pipeline, Settings (version/build/source commit from the shared sources), recovery-kit export through the Files mover with code reveal, Tokens.
  - iPad Pro 11-inch simulator: split shell with sidebar and detail pane renders.
  - Design brief was adversarially reviewed by three reviewers before implementation; their blocking points were folded in (Production CloudKit environment, backup exclusion, dladdr-based probe, scan cancel on device lock, non-restarting auto-refresh, stale temp purge, committed defaults xcconfig, `.swiftpm` ignore, Core-for-iOS CI step).
- NOT completed (stopped here on request):
  - Code review of the integrated iOS screens is incomplete. Five lens finders ran; the `state-correctness` finder and 55 of the refuters failed on the session usage limit, so only one finding is confirmed and every other finding below is raw and unverified. Nothing from this list has been fixed.
    - CONFIRMED (medium): revealed recovery code and revealed exchange secrets stay on screen into the app-switcher snapshot; `SuspensionCoordinator` does nothing on `.inactive`. Fix: opaque cover on `.inactive`, clear `revealedRecoveryCode` / `revealsAPIKey` / `revealsSecret` on resign-active (`RootView.swift`, `SettingsScreen.swift`, `ExchangesScreen.swift`).
    - UNVERIFIED security/privacy: the iOS build may run on Apple Silicon Macs and would then share the native Mac app's vault-key Keychain item (both use service `com.addressatlas.mac.vault`; `project.pbxproj:370`, consider `SUPPORTS_MACCATALYST`/`SUPPORTED_PLATFORMS` and a distinct service); backup exclusion is applied once after first unlock and skipped if the directory does not exist yet (`RootView.swift:43`); revealed recovery code is text-selectable so long-press copy bypasses the local-only pasteboard policy (`SettingsScreen.swift:278`).
    - UNVERIFIED UI/accessibility: `MainShell` swaps `TabShell`/`SplitShell` on size-class change and destroys screen state, possibly orphaning the export lane (`MainShell.swift:9`); Tokens decimal-pad fields lack a Done affordance (`TokensScreen.swift:608`); asset and snapshot rows pin the amount with `fixedSize`+`layoutPriority` without an accessibility-size fallback (`AssetsScreen.swift:334`); swipe-to-remove hard-codes `.white` on `AtlasTheme.loss` (`TokensScreen.swift:702`); "Address Atlas" title rendered twice on Unlock and in the iPad sidebar (`UnlockScreen.swift:28`); primary buttons on Wallets/Tokens/Exchanges are 42pt tall (`WalletsScreen.swift:114`); multi-line wallet field uses `submitLabel(.done)`+`onSubmit` which the vertical-axis field ignores (`WalletsScreen.swift:94`); Unlock headline truncates at accessibility sizes (`UnlockScreen.swift:82`); `PanelHeader` lacks the header trait for VoiceOver (`AtlasDesignSystem.swift:721`).
    - UNVERIFIED lifecycle/concurrency: the iCloud entitlement probe may fail closed on App Store/TestFlight installs if neither `__TEXT,__entitlements` nor a readable `embedded.mobileprovision` is present (`ICloudVaultService.swift:236`; must be proven on a real signed build); export lane never released if the Export/Settings screen is torn down mid-render (`ExportScreen.swift:222`); background-time assertion kept for a scan is only ended by the expiration handler (`RootView.swift:143`); in-flight iCloud transfer gets no background time (`RootView.swift:144`); auto-refresh loop captures a stale `scenePhase` (`RootView.swift:55`); background flush surfaces macOS "before quitting" wording on iOS and discards the result (`IOSSupport.swift:210`).
    - The `macOS regression` finder reported zero findings.
  - CI run 36072565996 for commit `bfb7b58` completed with an overall FAILURE. Per job: `Native iOS app` success (first-ever run of the new job), `Native macOS package` success, `Repository secret-artifact hygiene` success, `Deployment configuration` success, `Web and sync server` FAILURE about 35 seconds in with "Error response from daemon: No such container ..." (the Postgres service container was gone when the job tried to use it). This session changed no web or server code (only `package.json` scripts, docs, and native code), so the failure looks like a runner/Docker service problem, but that has NOT been proven: re-run the job and compare with the last green run of `f104aa1` before concluding.
  - Screen-agent follow-ups not done: promote the duplicated 44pt icon-button style and swipe-to-reveal rows into `IOSSupport.swift`; consider a List-backed layout for native swipe-to-delete; metric tiles do not deep-link to tabs.
  - Not exercised at all: Dynamic Type/VoiceOver passes, dark mode screenshots, adding an exchange connection with real credentials, iCloud save/restore (needs a signed, provisioned build and a physical device), recovery-kit restore round trip, any physical device.
- External gates (none claimed, none started): register App ID `com.addressatlas.ios` under `VWW3GZL279` with iCloud (existing container `iCloud.com.addressatlas.mac`) and Keychain Sharing (`com.addressatlas.mac`); iOS distribution profile; iOS App Store Connect record (then set `AddressAtlasUpdateURL`); signed device build; Mac-to-iPhone iCloud restore with iCloud Keychain on both. There is no iOS record, upload, TestFlight, App Review, or publication.
- Memory: added `memory/decisions/2026-09-25-ios-app-target.md`; updated `memory/file-map/2026-07-20-native-ownership-map.md`; `memory:sync` and `memory:doctor` clean. No note was added for the unverified review findings because none is confirmed beyond the snapshot exposure.
- Next: start from the CONFIRMED finding and the UNVERIFIED lists above (re-run a verifier pass on them), re-read CI run 36072565996, then the external Apple gates.
