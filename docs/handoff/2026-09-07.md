## 2026-09-07 01:56 +03 - Codex

- Task: Replace native hosted-account backups with optional private iCloud portfolio copies without buying an API plan.
- Changed: Added CloudKit save/restore/delete and synchronizable-Keychain encryption keys; rewrapped exchange credentials across Mac keys; authenticated account/revision and conditional writes; preserved rollback and legacy journal migration. Removed the server/passkey UI and production automatic server-config dependencies. Updated privacy, store packet, screenshots, signing profile validation, and iCloud setup docs. Legacy server source is retained for compatibility tests; no deployed database or backup was deleted.
- Verified: Full Swift suite 500 tests, 3 expected skips, 0 failures under normal and strict-concurrency/warnings-as-errors runs. Follow-up codec/workflow/storage tests 36/36. Four provisioning-grant tests and MAS contracts passed. Universal arm64+x86_64 ad-hoc sandboxed app built and artifact validation passed (synthetic store ID used only locally). Five store screenshots regenerated; iCloud screenshot visually inspected. Secret-artifact/diff checks passed; repo memory healthy, 27/27 notes.
- Memory: Added private-iCloud decision and updated the older MAS release boundary. The default CloudKit zone uses one conditional record write with atomically:false, not an unsupported atomic batch.
- Next: Apple Developer sign-in is required (the in-app browser is at the login page; cktool has no management token). Existing development identity/profile belongs to other provisioning scope; Address Atlas Production CloudKit container/schema and distribution profile/certificates remain unconfigured/unverified. Follow `docs/ICLOUD.md`, test signed transfers and Keychain delivery on two Macs, then complete App Store submission. Do not claim live iCloud, server deletion, App Review submission, or publication. Free price/RPC integrations were preserved; usage permissions remain a separate release check.

## 2026-09-07 - Codex - Chrome Apple setup

- Task: Resume Apple submission setup through the user's signed-in Chrome session.
- Changed: Registered `com.addressatlas.mac` and associated the new `iCloud.com.addressatlas.mac` container under verified team `VWW3GZL279`. Created `EncryptedVault` fields (`payload`: Asset, `keyID` and `revision`: String), removed its default world read access, enabled creator read/write, retained iCloud create-only, and deployed schema to Production. Created macOS App Store Connect record `6809515105`, SKU `address-atlas-macos-001`, English US. Apple currently defaults the draft version to 1.0; change to planned 0.2.0 before upload. Updated release documentation with actual identifiers.
- Verified: CI run `34065435187` completed successfully for `a67f3e7`. Apple re-read shows one assigned iCloud container; CloudKit reported Changes Deployed. ASC record exists in Prepare for Submission, with no build uploaded. Local signing still has only Apple Development; portal additionally has Distribution Managed, not a usable local app/installer identity.
- Blocker: Mac App Distribution CSR prepared in an owner-private, repo-external local cache; no private material committed or sent. Chrome filechooser upload failed with Not allowed because the extension needs Allow access to file URLs. User must enable that extension permission. Do not bypass the browser restriction. Existing app/installer certificates were not revoked or replaced.
- Next: Continue the open certificate CSR upload after permission is enabled; create/install app and installer distribution identities, generate matching profile, finish metadata/version/pricing, signed two-Mac tests and provider-permission gates, then validate/upload. No App Review submission, public release, real iCloud transfer, or old server data deletion is claimed. Chrome Apple Developer, ASC, and CloudKit tabs are retained for handoff.
- Memory: none; this entry records external provisioning state, not a change to the app architecture.

## 2026-09-07 - Codex - Certificate upload retry

- Task: Retry after the user enabled Chrome local-file access.
- Result: Reconnected to restarted Chrome (browser 3, same Apple tab). CSR upload succeeded; Apple created Mac App Distribution certificate `TBHQS7MJMV`, expiring 2027-09-07, under team `VWW3GZL279`.
- Blocker: Clicking Apple's Download link displayed `developer.apple.com engellendi`, `ERR_BLOCKED_BY_CLIENT`. No certificate file appeared in Downloads; no alternate download route or security-interstitial bypass was attempted. The user must resolve Chrome's download block. Certificate is issued, not installed; original owner-private local signing key remains outside the repo for continuation. Do not create another app certificate on resume.
- Next: Download this existing certificate once Chrome permits it, match/import with the existing local key, then continue installer certificate/profile and the remaining release gates above. No app build uploaded.
