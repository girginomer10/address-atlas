# Handoff — 2026-07-21

## 2026-07-21 00:01 +03 - Codex

- Task: Followed the L6 hardening push through CI, reproduced the real PostgreSQL 16 restore failure, and closed the restore, credential-logging, atomic-recovery, and test-harness gaps it exposed.
- Changed: restore/drill provisioning now transactionally accepts only the clean `template0` owner states and normalizes `public` to `address_atlas`, while steady mode and unknown owners fail closed; bootstrap/restore credentials move through temp-table `COPY` data rather than SQL statement text, with fixed secret-safe diagnostics and tracing/collector guards; backup hooks preserve reviewed stderr markers while suppressing stdout; CI now proves an encrypted drill plus a data-mutating atomic production restore, distinct safety backup, cleanup, and post-restore health; release-doctor and operations doubles are stable across macOS XProtect, TTY input, Bash 3.2, and Bash 5.
- Verified: `npm test` 325 passed / 35 intentionally skipped; focused release-doctor tests passed 7/7 twice in 3.6–4.2 seconds; real PostgreSQL 16 role/restore/log-leak matrix passed; operations matrix passed 56/56 under both Bash 3.2 and Bash 5.3; typecheck, production build, `npm audit --audit-level=low` (0 vulnerabilities), shell syntax, workflow YAML parse, and `git diff --check` passed. Independent final audits found no concrete blocker; stock local PostgreSQL does not load real pgAudit/auto_explain extensions, so those settings are guard-checked here and remain production-extension evidence rather than a local execution claim.
- Memory: added `memory/gotchas/2026-07-20-template0-public-schema-owner.md`, `memory/gotchas/2026-07-20-role-credential-statement-logging.md`, and `memory/gotchas/2026-07-20-process-accurate-test-doubles.md`.
- Environment: local validation installed Homebrew Docker CLI 29.6.2, Colima 0.10.3, and age 1.3.1. No Colima VM/profile, container, volume, temporary credential, or disposable PostgreSQL cluster was left running or retained.
- Next: commit and push this follow-up, then require the replacement GitHub CI run—including the destructive fresh-volume bootstrap and atomic production-restore proof—to finish green. External release evidence still requires real pager delivery, offsite recovery keys, a timed cross-host RTO drill, Apple signing/notarization credentials, and explicit approval before changing live GitHub governance settings.

## 2026-07-21 00:10 +03 - Codex (CI follow-up)

- Task: Investigated the first full atomic production-restore CI execution after `231f062`; all other jobs passed, but the pinned PostgreSQL helper container failed before provisioning with `/bin/sh: can't open '/opt/address-atlas/provision-runtime-role.sh': Permission denied`.
- Root cause: the official PostgreSQL entrypoint drops to its unprivileged `postgres` user before the supplied command. The operator-owned snapshot was mode `0500`, so the container user could not read the read-only bind mount; the host directory's mode `0700` did not grant access to the mounted file.
- Changed: retained the private `0700` staging directory and every container hardening boundary, but made the non-secret, digest-verified helper snapshots mode `0555`; extended the hook test to capture and assert both file mode `555` and parent mode `700` at the Docker invocation boundary.
- Verified: the focused restore-provision hook suite passed 4/4; full `npm test` passed 325 / 35 intentionally skipped; typecheck, production build, `npm audit --audit-level=low` (0 vulnerabilities), shell syntax, workflow YAML parse, `git diff --check`, and semantic-memory doctor passed. Commit and push next, then require a replacement CI run to prove both atomic restore and fresh-volume bootstrap in the pinned container images.

## 2026-07-21 00:18 +03 - Codex (fresh-volume CI follow-up)

- Task: Followed `b1f114d` through CI. The complete atomic production restore passed, then the first reachable destructive fresh-volume bootstrap failed before artifact inspection with `Expected exactly one address-atlas-sync/postgres container; found 0`.
- Root cause: `inspect` deliberately performs live encrypted-backup verification in addition to parsing metadata. The workflow had not explicitly bound `ADDRESS_ATLAS_POSTGRES_CONTAINER` to the GitHub source service before inspection, and that service has no Compose labels for fallback discovery.
- Changed: bind the source container before artifact inspection, retain the existing explicit rebind to the fresh target after source destruction, and add a workflow-contract test pinning the full source-bind → inspect → source-remove → target-bind → classify order.
- Verified: focused workflow contract 1/1 and full `npm test` 326 / 35 intentionally skipped; typecheck, production build, `npm audit --audit-level=low` (0 vulnerabilities), workflow YAML parse, `git diff --check`, and semantic-memory doctor passed. Commit and push next, then require the replacement CI run to finish every job green.
