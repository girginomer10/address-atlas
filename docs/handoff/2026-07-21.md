# Handoff — 2026-07-21

## 2026-07-21 00:01 +03 - Codex

- Task: Followed the L6 hardening push through CI, reproduced the real PostgreSQL 16 restore failure, and closed the restore, credential-logging, atomic-recovery, and test-harness gaps it exposed.
- Changed: restore/drill provisioning now transactionally accepts only the clean `template0` owner states and normalizes `public` to `address_atlas`, while steady mode and unknown owners fail closed; bootstrap/restore credentials move through temp-table `COPY` data rather than SQL statement text, with fixed secret-safe diagnostics and tracing/collector guards; backup hooks preserve reviewed stderr markers while suppressing stdout; CI now proves an encrypted drill plus a data-mutating atomic production restore, distinct safety backup, cleanup, and post-restore health; release-doctor and operations doubles are stable across macOS XProtect, TTY input, Bash 3.2, and Bash 5.
- Verified: `npm test` 325 passed / 35 intentionally skipped; focused release-doctor tests passed 7/7 twice in 3.6–4.2 seconds; real PostgreSQL 16 role/restore/log-leak matrix passed; operations matrix passed 56/56 under both Bash 3.2 and Bash 5.3; typecheck, production build, `npm audit --audit-level=low` (0 vulnerabilities), shell syntax, workflow YAML parse, and `git diff --check` passed. Independent final audits found no concrete blocker; stock local PostgreSQL does not load real pgAudit/auto_explain extensions, so those settings are guard-checked here and remain production-extension evidence rather than a local execution claim.
- Memory: added `memory/gotchas/2026-07-20-template0-public-schema-owner.md`, `memory/gotchas/2026-07-20-role-credential-statement-logging.md`, and `memory/gotchas/2026-07-20-process-accurate-test-doubles.md`.
- Environment: local validation installed Homebrew Docker CLI 29.6.2, Colima 0.10.3, and age 1.3.1. No Colima VM/profile, container, volume, temporary credential, or disposable PostgreSQL cluster was left running or retained.
- Next: commit and push this follow-up, then require the replacement GitHub CI run—including the destructive fresh-volume bootstrap and atomic production-restore proof—to finish green. External release evidence still requires real pager delivery, offsite recovery keys, a timed cross-host RTO drill, Apple signing/notarization credentials, and explicit approval before changing live GitHub governance settings.

## 2026-07-21 00:10 +03 - Codex (CI follow-up)

- Task: Investigated the first full atomic production-restore CI execution after `231f062`; all other jobs passed, but the pinned PostgreSQL helper container failed before provisioning with `/bin/sh: can't open '/opt/address-atlas/provision-runtime-role.sh': Permission denied`.
- Root cause: the official PostgreSQL entrypoint drops to its unprivileged `postgres` user before the supplied command. The operator-owned snapshot was mode `0500`, so the container user could not read the read-only bind mount; the host directory's mode `0700` did not grant access to the mounted file.
- Changed: retained the private `0700` staging directory and every container hardening boundary, but made the non-secret, digest-verified helper snapshots mode `0555`; extended the hook test to capture and assert both file mode `555` and parent mode `700` at the Docker invocation boundary.
- Verified: the focused restore-provision hook suite passed 4/4; full `npm test` passed 325 / 35 intentionally skipped; typecheck, production build, `npm audit --audit-level=low` (0 vulnerabilities), shell syntax, workflow YAML parse, `git diff --check`, and semantic-memory doctor passed. Commit and push next, then require a replacement CI run to prove both atomic restore and fresh-volume bootstrap in the pinned container images.

## 2026-07-21 00:18 +03 - Codex (fresh-volume CI follow-up)

- Task: Followed `b1f114d` through CI. The complete atomic production restore passed, then the first reachable destructive fresh-volume bootstrap failed before artifact inspection with `Expected exactly one address-atlas-sync/postgres container; found 0`.
- Root cause: `inspect` deliberately performs live encrypted-backup verification in addition to parsing metadata. The workflow had not explicitly bound `ADDRESS_ATLAS_POSTGRES_CONTAINER` to the GitHub source service before inspection, and that service has no Compose labels for fallback discovery.
- Changed: bind the source container before artifact inspection, retain the existing explicit rebind to the fresh target after source destruction, and add a workflow-contract test pinning the full source-bind → inspect → source-remove → target-bind → classify order.
- Verified: focused workflow contract 1/1 and full `npm test` 326 / 35 intentionally skipped; typecheck, production build, `npm audit --audit-level=low` (0 vulnerabilities), workflow YAML parse, `git diff --check`, and semantic-memory doctor passed. Commit and push next, then require the replacement CI run to finish every job green.

## 2026-07-21 00:31 +03 - Codex (workflow shell follow-up)

- Task: Followed `e1f2834` through CI. Repository hygiene, deployment, native packaging, the complete web suite, and atomic production restore all passed; the now-reachable fresh-volume bootstrap then failed while parsing the exported recovery function.
- Root cause: the workflow was valid YAML, but both quoted heredoc terminators retained two shell-level spaces after YAML dedent. Bash therefore consumed the rest of the exported function while looking for `SQL` and failed at the unmatched command substitution.
- Changed: align both terminators to column zero after YAML dedent; extend the workflow contract to normalize line endings, pin exactly two heredoc opening/closing pairs, extract the actual recovery `run: |` body, and require `bash -n` success.
- Verified: focused workflow contract passed 2/2; full `npm test` passed 327 / 35 intentionally skipped; typecheck, production build, `npm audit --audit-level=high` (0 vulnerabilities), workflow YAML and extracted-shell parsing, and `git diff --check` passed. An independent audit found no blocker and prompted the stricter delimiter-count and block-boundary assertions.
- Memory: added `memory/gotchas/2026-07-21-workflow-shell-dedent.md`.
- Next: sync and validate semantic memory, commit and push, then require the replacement CI run—including destructive fresh-volume bootstrap—to finish green.

## 2026-07-21 00:38 +03 - Codex (native-config receipt follow-up)

- Task: Followed `61d9bca` through CI run `29780627882`. Every other job and web step passed, including native Swift/TSan/universal packaging, production image checks, atomic production restore, fresh-volume restore, restored data, and database-role contracts; bootstrap reached the final native-config receipt gate.
- Root cause: the final receipt fetched the ordinary cacheable `/config/native` response, while `verify-response` correctly requires the `no-store` response used for revision-bound deployment probes. The body fingerprint matched, but cache freshness/provenance did not satisfy the receipt contract.
- Changed: fetch the recovered service through `?deployment_probe=${CI_NATIVE_REVISION}` and pin that exact uncached, revision-bound request in the CI recovery workflow contract.
- Verified: focused workflow and native-config route suites passed 6/6; full `npm test` passed 328 / 35 intentionally skipped; typecheck, production build, `npm audit --audit-level=high` (0 vulnerabilities), workflow YAML parse, extracted-shell `bash -n`, `git diff --check`, and semantic-memory doctor 18/18 passed. An independent audit confirmed the probe fix is exact and preserves both client caching and revision verification.
- Memory: added `memory/gotchas/2026-07-21-native-config-deployment-probe.md`.
- Next: commit and push, then require the replacement CI run to finish green.

## 2026-07-21 00:45 +03 - Codex (green CI closure)

- Task: Closed the end-to-end CI proof for the L6 hardening series.
- Result: commit `dd2ee2c` passed CI run `29781130995` in full: repository secret-artifact hygiene, deployment operations, all web/sync checks, real PostgreSQL role contracts, exact production image, encrypted backup/drill, atomic production restore, destructive fresh-volume bootstrap/finalization, Swift tests, Thread Sanitizer, release compilation, and universal signed bundle validation.
- Changed: after the green run, parameterized the native-config route unit test over both supported uncached probe aliases (`release_probe` and `deployment_probe`) so the workflow contract and route behavior cannot drift independently.
- Verified: focused workflow and route suites passed 7/7; full `npm test` passed 329 / 35 intentionally skipped; typecheck, `git diff --check`, and semantic-memory doctor 18/18 passed. Parent production code commit is fully green in GitHub CI. No semantic-memory change; the deployment-probe rule is already captured in `memory/gotchas/2026-07-21-native-config-deployment-probe.md`.
- Next: commit and push the test-only hardening, then require its replacement CI run to remain green.

## 2026-07-21 03:08 +03 - Codex (L6 product and trust-boundary closure)

- Task: Validated Claude's quality audit against the current repository, ran an independent full-diff red team, and remediated every confirmed P0-P2 code/product finding rather than treating prior wiring work as the finish line.
- Changed: added an encrypted durable pending-upload journal with atomic CAS recovery; made endpoint-config trust publication transactional; bound Solana token records to wallet/program/type and aggregated each mint with exact `UInt64` semantics; pinned XRP native/trust-line reads to one validated ledger and EVM native/ERC-20 reads to one exact block; made passkey browser outcomes bounded, privacy-safe, and capacity-limited before body reads; made PostgreSQL readiness exact across schema/ACL/defaults/object surfaces and fail before mutating drifted predecessor migrations; bound release publication to the exact CI run/attempt/check-run/App identities with a second prepublish proof; added bounded notarization, backup-config freshness, strict Swift gates, production bundle metadata, and a generated 1024px app icon.
- Changed files: `.github/workflows/{ci,release}.yml`; `src/app/auth/**`; `src/lib/sync/**`; `server/sync/**`; native storage/sync/scanner/AppState/UI/build/notary code and tests; `native/AddressAtlasMac/Resources/AppIcon.png`.
- Verified: real PostgreSQL 16 full Vitest matrix 411/411; TypeScript, Next production build, and `npm audit --audit-level=high` (0 vulnerabilities); operations 58/58; notary 9/9; build-version 61/61; full Swift 304 passed with 2 opt-in live tests skipped; strict concurrency + warnings-as-errors; Thread Sanitizer 304 passed with no race finding; shell syntax, workflow YAML parse, scoped Swift formatting, line-size check, and `git diff --check`. Built `dist/Address Atlas.app` as universal `x86_64 arm64`, valid ad-hoc hardened-runtime signature, version 0.2.0 (build 61), and complete ICNS metadata.
- Memory: added `memory/bugfixes/2026-07-21-l6-data-and-release-boundaries.md`; sync/doctor still required after this entry.
- Open evidence: macOS visual QA is pending because the Mac was locked; no live XRPL smoke request was made. Public DNS/deploy, GitHub rulesets/environments/security governance, Developer ID/notary credentials, pager delivery, offsite recovery keys, and a timed cross-host RTO drill remain external release gates and were not changed without owner authority.
- Next: sync/doctor semantic memory, stop the disposable PG16 cluster, commit/push `main`, require replacement CI green, rerun strict release-doctor, and resume packaged-app visual QA after manual Mac unlock.

## 2026-07-21 03:16 +03 - Codex (exact-schema CI fixture follow-up)

- Task: Investigated CI run `29789512413`, where web tests/build and three jobs passed but the database-role contract fixture failed after new exact-schema enforcement.
- Root cause: the role-boundary test deliberately left owner-only sequence/type/function objects in `public` before asserting a no-op bootstrap, which now correctly rejects any expanded application schema. Its non-production drill/restore databases also needed the explicit restore-migration boundary required by production config validation.
- Changed: prove runtime denial while adversarial objects exist, assert bootstrap rejects that drift, remove only the fixture objects, and then prove no-op bootstrap; bind the test helper's allowed `atlas_drill_*`/`atlas_restore_*` databases to `ADDRESS_ATLAS_RESTORE_MIGRATION=1` while the real production database remains unflagged.
- Verified: the complete real PostgreSQL 16 `database-role-tests.sh` fresh/legacy/rotation/drill/restore matrix passed locally. Next: memory sync/doctor, amend with a follow-up commit, push, and require replacement CI fully green.

## 2026-07-21 03:39 +03 - Codex (restore search-path contract follow-up)

- Task: Followed commit `dd10b34` through CI run `29789909262`. Repository hygiene, deployment configuration, native Swift/strict-concurrency/TSan/universal packaging, and every web step through the exact role matrix and production image passed; only the encrypted restore drill failed in its migration/readiness hook.
- Root cause: the production pool correctly configured raw `search_path=public`, which leaves `pg_catalog` implicit and therefore first, but `restore-readiness.ts` incorrectly demanded the unsafe literal `public,pg_catalog`. Unit tests covered only the pool half of that contradictory contract and the backup wrapper intentionally redacted the inner error.
- Changed: centralized the production search-path contract; require exact current/session owner, `current_schema=public`, raw/explicit path `public`, effective path `pg_catalog,public`, no owner-named spoof schema, and catalog-qualified probe functions/operators. Added unit fail-closed permutations plus a real PostgreSQL shadow-function integration proving the implicit order is safe and the explicit reversed order is exploitable and rejected. Kept both runtime and owner pools on the safe implicit-catalog option.
- Verified: real PostgreSQL 16 full suite 45 files / 425 tests; focused unit 24/24; new PG16 integration 2/2; an actual `pg_dump --no-owner --no-privileges` into fresh `template0`, bundled `sync-restore.cjs`, and readiness bootstrap passed; typecheck, production build, dependency audit with zero vulnerabilities, operations 58/58, and `git diff --check` passed.
- Memory: updated `memory/bugfixes/2026-07-21-l6-data-and-release-boundaries.md` with the implicit-catalog invariant. Next: sync/doctor, commit/push, and require a replacement CI run to pass all four jobs including encrypted restore and destructive fresh-volume recovery.

## 2026-07-21 06:10 +03 - Codex (independent L6 product and correctness follow-up)

- Task: Validated Claude's quality review against the current repository, then ran an independent changed-file red team for code correctness, product durability, accessibility, abuse resistance, and provider-data trust.
- Changed: moved durable registration admission to the successful WebAuthn/account transaction; added privacy-safe operational error codes and SQL-guarded stored-vault integrity checks on GET/restore; made authenticated ingress reject exhausted budgets before body receipt and commit actual received bytes without quota or delete-race bypass; kept the schema at migration head v3 after proving that a one-release v4 constraint bump would break N/N-1 rollback; held bounded GET permits through response drain/cancel and charged exact encoded egress bytes at account/client/global scopes; bound Bitcoin/TRON/Cosmos/Binance responses to identity and duplicate-conflict rules; added bounded HTTP retries and non-amplifying malformed EVM batch handling; made Solana SOL/Token/Token-2022 reads converge on one exact returned slot with raw `UInt64` lamports; gave shared endpoint refreshes independently cancellable waiter leases and cancellation-safe trust publication; made Cmd-Q wait for active persistence and flush both wallet-label drafts and in-memory post-sync candidates; made JSON/CSV export reflect visible drafts without mutating app state and moved serialization off MainActor; improved narrow-window, Dynamic Type, contrast, and destructive-control accessibility behavior.
- Verified: production-pinned PostgreSQL 16.14 full web matrix 46 files / 478 tests; TypeScript, Next production build, and high-severity dependency audit with zero vulnerabilities; operations 58/58; full PostgreSQL role/restore matrix; Caddy/deployment contracts 12/12; notarization credential/timeout and build-version harnesses; full Swift 358 tests with 2 opt-in live tests skipped under strict concurrency/warnings-as-errors and Thread Sanitizer; focused cancellation/convergence/termination suites; tracked YAML parse, scoped Swift format checks, and `git diff --check`. Built `dist/Address Atlas.app` before commit as universal `x86_64 arm64`, version 0.2.0 build 64, with complete ICNS metadata and a valid ad-hoc hardened-runtime signature. Live read-only checks validated all four built-in Cosmos chain identities and height-pinned bank reads; Osmosis/Celestia correctly exercised the no-echo-header fallback contract.
- Memory: updated `memory/bugfixes/2026-07-21-l6-data-and-release-boundaries.md` with ingress/egress accounting, rollback-compatible schema evolution, exact Solana slot convergence, shared-task ownership, termination durability, and stored-snapshot integrity invariants.
- Open evidence: source-only UI checks passed, but real narrow-window/VoiceOver visual QA remains pending because the Mac session is locked. Public DNS/deployment, GitHub governance, Developer ID/notarization credentials, pager delivery, offsite recovery custody, and a timed cross-host RTO drill remain owner-controlled release gates.
- Next: sync/doctor repo memory, commit and push `main`, require replacement GitHub CI fully green, and resume packaged-app visual QA after manual Mac unlock.

## 2026-07-21 07:24 +03 - Codex (final L6 integrity follow-up)

- Task: Completed the last independent pass over local-vault safety, remote rollback UX, exact exchange accounting, cross-process nonce durability, rolling PostgreSQL compatibility, and deploy availability.
- Changed: hardened the SQLite directory/file identity and allocation boundaries; added encrypted full-document checkpoints whose restore reapplies prior user content while retaining the entire current sync authority/baseline and that are discarded before account identity changes; introduced exact bounded decimal exchange quantities and fail-closed alias aggregation; added real isolated-Keychain multi-process Kraken nonce/crash coverage; kept schema head v3 while preparing checksum-bound v4 for the N/N-1 window; guarded PostgreSQL values physically before logical projection and made both current convergence and future v4 activation skip no-op access-exclusive table locks; made sync activity, account switching, status announcements, exports, dust disclosure, and narrow-window geometry explicit and accessible; updated `tldts` to 7.4.9.
- Verified: full web/operations suite 432 passed / 50 environment or live tests skipped; typecheck, production build, dependency audits (0 vulnerabilities), operations 58/58, PostgreSQL 16.14 migration/vault 27/27 and readiness 6/6; full native 384 tests with 2 opt-in skips under normal and strict concurrency/warnings-as-errors, and 384 with one additional intentional nested-process skip under TSan; focused SQLite/Kraken process tests 25/25; scoped Swift formatting and diff whitespace clean. Local Caddy container validation was unavailable because no Docker daemon was running; the unchanged Caddy boundary remains covered by repository contracts and must be proved again by GitHub CI.
- Memory: updated `memory/bugfixes/2026-07-21-l6-data-and-release-boundaries.md` with rollback-discard, SQLite topology/bounds, exact decimal, cross-process nonce, prepared migration, and physical-before-logical PostgreSQL invariants.
- External gates: strict release-doctor still lacks Developer ID signing, production environment, code-sign identity, and notarization credentials; the checkout is intentionally dirty until this commit. The console remains locked, so fresh packaged-app visual/VoiceOver QA is pending. Public resolvers return no record for `sync.addressatlas.app`. The public GitHub repository has no `main` branch protection, ruleset, or deployment environment; Dependabot security updates are disabled. No owner-controlled setting or live deployment was changed.
- Next: sync and validate semantic memory, commit and push `main`, require the replacement CI run to finish green, then resume visual QA and release operations only after the owner supplies/approves the external gates.

## 2026-07-21 08:14 +03 - Codex (pre-commit rollback and deploy red-team closure)

- Task: Closed the final independent P0-P2 pass over rollback restoration, recovery-kit discovery, prepared migration locking, and automatic deploy/restore rollback safety.
- Changed: rollback checkpoints remain full-document encrypted artifacts at rest, but restoration atomically reapplies their user content over the complete current `SyncState`, so account/session/remote-version/checksum authority is never rewound and download → restore → upload advances normally; recovery-kit unlock now republishes checkpoint availability; UI copy states that content and credentials return while the current sync account/baseline remains. Prepared v4 conditionally skips an already-converged storage `ALTER`, adds its large-table constraint `NOT VALID`, and leaves validation for v5 so an access-exclusive lock is not held through a scan. Production images publish exact cumulative migration-chain labels; deploy and restore read the current head from a verified signed live backup, require `max(current, active)` exact-chain compatibility, reject partial/mismatched labels and unsafe skipped releases, and permit legacy unlabeled rollback only when the database is unchanged at the active head. The verified frontend remains available during the transaction-consistent, schema-lock-protected pre-deploy backup.
- Verified: full web suite 40 files passed / 6 skipped, 444 tests passed / 50 skipped; deployment/migration focused matrix 69/69; typecheck and production build; development and production dependency audits with zero vulnerabilities; operations 58/58; real disposable PostgreSQL 16.14 migration/vault 27/27 and readiness 6/6; full native 385 tests with 2 opt-in skips in normal and strict-concurrency/warnings-as-errors modes, and 385 with 3 intentional skips under Thread Sanitizer and no race report; focused SQLite rollback 24/24 and AppState network/recovery 45/45; scoped Swift format, shell syntax, and diff whitespace clean. The disposable PostgreSQL clusters were stopped and removed without touching Meridian's live cluster.
- Memory: corrected rollback semantics and added the split constraint-validation, exact migration-chain image capability, and live safety-backup invariants in `memory/bugfixes/2026-07-21-l6-data-and-release-boundaries.md`.
- External gates: local Docker/Caddy runtime proof and fresh packaged VoiceOver QA remain pending; the console is locked. Developer ID/notarization credentials, production environment, public DNS/deploy, GitHub governance, pager delivery, offsite recovery custody, and a timed cross-host RTO drill remain owner-controlled and were not changed.
- Next: sync/doctor semantic memory, commit and push `main`, require replacement GitHub CI—including Docker/Caddy, atomic restore, fresh-volume recovery, strict Swift, TSan, and packaging—to finish green, then rerun the clean-tree release doctor.

## 2026-07-21 08:28 +03 - Codex (fresh-volume readiness CI follow-up)

- Task: Followed commit `8d12d33` through CI run `29803558403`; hygiene, deployment/Caddy, production image, encrypted backup/drill/atomic restore, native normal/strict/TSan, release compilation, and universal signed packaging passed, but destructive fresh-volume recovery failed before bootstrap classification.
- Root cause: the official PostgreSQL entrypoint briefly starts a socket-only temporary postmaster while initializing an empty volume. The workflow's socket-only `pg_isready` latched that process; the entrypoint then stopped it before execing the final PID 1 server, so `classify-source` landed in the intentional no-socket gap.
- Changed: fresh-volume readiness now requires `/proc/1/comm` to be exactly `postgres` and a successful TCP probe on `127.0.0.1` before target binding or classification. The same final-PID/TCP contract now protects the GitHub service container plus development and production Compose healthchecks. Workflow and deployment contracts pin the gate and its ordering.
- Verified: focused workflow/deployment contract 3/3; full web suite 40 files passed / 6 skipped, 447 tests passed / 50 skipped; typecheck; workflow and both Compose YAML files parse; operations 58/58; shell/diff whitespace clean. Local Docker Compose validation remains unavailable because this Mac has neither a Compose plugin nor a running daemon; the replacement CI run is the authoritative container proof.
- Memory: added `memory/gotchas/2026-07-21-postgres-entrypoint-transient-readiness.md`.
- Next: sync/doctor memory, commit and push the follow-up, and require the replacement CI run's fresh-volume recovery plus all previously green jobs to finish green.

## 2026-07-21 10:24 +03 - Codex (L6 adversarial closure)

- Task: Revalidated Claude's quality findings against current `main`, completed two independent adversarial passes, and fixed the remaining backend, WebAuthn, native policy, lifecycle, provider-integrity, accessibility, and release-script gaps.
- Changed: stored passkeys now use one explicit ES256/RS256 policy with exact COSE fields, cryptographic import, bounded pre-detoast reads, and restore rejection for invalid or credentialless accounts; passkey verification has pool-aware global/client/credential limits; storage-ledger reconciliation is a bounded asynchronous repeatable-read audit whose sticky drift marker gates writes without taking auth/reads down; production database URLs reject partial TLS parameter sets unless `sslmode=verify-full` is explicit; the native bridge is inert before hydration and returns actionable privacy-safe retry states; endpoint policy fallback is current-process and exact-authority only, post-rename durability uncertainty blocks sensitive work until re-fsync, pending-upload recovery revalidates policy, and detach/checkpoint cleanup is atomic after remote revocation; Cosmos/TRON duplicate and snapshot-height conflicts fail closed; public notarization requires a universal arm64/x86_64 bundle.
- Verified: focused backend regression 59/59; full Vitest suite against disposable PostgreSQL 16 passed twice, including shuffled seed `7212026`, at 50 files / 562 tests / 0 skips; typecheck, Next production build, `npm audit --audit-level=high` with 0 vulnerabilities, and `git diff --check` passed; operations passed 58/58; strict Swift concurrency plus warnings-as-errors passed 399 tests with 2 opt-in live skips and no failures; Thread Sanitizer passed all 399 with one additional documented nested-process skip and no race report; release Swift product compiled; notary harness passed 11/11 and build-version harness passed 67 checks. Browser QA proved no pre-hydration form submission, preserved callback/state, correct button/ARIA state, Enter-key behavior, and fail-closed invalid callback/state UX. Independent final backend/web and native/release blocker audits found no concrete release-blocking issue.
- Environment: the exact disposable PostgreSQL container and temporary Docker config were removed, and the locally started Colima VM was stopped. No test database or credential remains.
- Memory: updated `memory/bugfixes/2026-07-21-l6-data-and-release-boundaries.md` with passkey-policy, verification-capacity, asynchronous ledger, endpoint-durability, lifecycle, provider-snapshot, universal-bundle, and pre-hydration invariants.
- External gates: release-doctor passes its local tooling checks but still fails on missing Developer ID identity, unset signing identity, and absent notary credentials; production environment and live exchange smoke credentials are warnings. Public DNS/deploy, GitHub governance, pager delivery, offsite recovery custody, timed cross-host RTO proof, and packaged VoiceOver QA on the locked console remain owner-controlled evidence.
- Next: sync/doctor semantic memory, commit and push `main`, require replacement GitHub CI fully green, and do not call the public release complete until the external gates are supplied and proved.

## 2026-07-21 10:43 +03 - Codex (catalog-matrix CI timing follow-up)

- Task: Followed commit `d6e63d8` through CI run `29810995109`; repository hygiene, deployment/Caddy, and the complete native normal/strict/TSan/release/universal-package job passed, but the web job stopped in the runtime-role readiness integration matrix.
- Root cause: the single test intentionally performs dozens of serial role/ACL mutations and exact catalog scans. Under shared-runner load it reached 5.007 seconds and inherited Vitest's 5-second unit-test default; it had no failed assertion or production timeout, and the file continued cleanup while every other test passed.
- Changed: gave only that adversarial catalog matrix an explicit finite 20-second budget. Global test defaults and all production statement/lock/query timeouts remain unchanged.
- Verified: the focused real-PostgreSQL readiness file passed 7/7 in 2.19 seconds; the complete real-PostgreSQL suite then passed 50 files / 567 tests / 0 skips under parallel file load. The exact disposable container was removed and Colima stopped.
- Memory: added `memory/gotchas/2026-07-21-adversarial-catalog-test-timeout.md`.
- Next: sync/doctor semantic memory, commit and push the follow-up, and require the replacement GitHub CI run—including the previously skipped role/bootstrap/restore/image stages—to finish green.

## 2026-07-21 10:54 +03 - Codex (restore-credential fixture follow-up)

- Task: Followed commit `52f73f8` through CI run `29811592582`; all native, deployment, hygiene, web tests, typecheck, build, schema, role, runtime-provisioning, and exact-image stages passed, but the encrypted restore drill failed its migration/readiness hook.
- Root cause: the recovery fixture still stored base64url text for `ci-public-key`, not an importable WebAuthn COSE public key. The new production restore boundary correctly rejected that otherwise unreachable restored account.
- Changed: replaced the placeholder with a valid ES256/P-256 COSE public key and added a workflow-contract test that extracts the seeded credential ID/key and runs the same production validators before CI reaches the destructive restore drill.
- Verified: focused recovery-workflow contract passed 6/6; the full local Vitest suite passed 44 files / 517 tests with 6 environment-gated files / 51 tests skipped; TypeScript typecheck, `git diff --check`, and semantic-memory doctor 22/22 passed.
- Memory: added `memory/gotchas/2026-07-21-restore-fixture-passkey-integrity.md`.
- Next: sync/doctor semantic memory, commit and push the follow-up, then require a replacement CI run to pass encrypted restore, destructive fresh-volume recovery, and all other jobs.

## 2026-07-21 11:03 +03 - Codex (green L6 CI closure)

- Task: Closed the replacement CI proof after fixing the production-shaped restore credential fixture.
- Result: commit `982d5d2` passed GitHub Actions run `29812356527` in full: repository artifact hygiene, deployment/Compose/Caddy, 568-test web/PostgreSQL matrix, typecheck, production build, exact schema and role provisioning, exact production image, encrypted backup/drill/atomic restore, destructive fresh-volume recovery, Swift normal/strict/TSan suites, release compilation, and universal signed bundle validation.
- Memory: marked the restore-fixture rule's end-to-end GitHub Actions proof complete.
- Next: no code follow-up. Public release still requires the owner-controlled signing/notary, DNS/deployment, pager/offsite-recovery/RTO, governance, and packaged VoiceOver evidence listed in the 10:24 handoff.

## 2026-07-21 15:08 +03 - Codex (full product-quality closure)

- Task: Revalidated Claude's quality-bar review against the current product, implemented every confirmed P0-P2 finding, and repeated independent adversarial reviews until the current diff had no remaining confirmed P0-P2.
- Changed: added explicit damaged-vault quarantine/rollback recovery with cross-process file leases; privacy-safe diagnostics and share-safer default exports; exact native vault, exchange, provider-snapshot, token-expiry, cancellation-generation, accessibility, and single-instance invariants; shared pool-aware auth admission and parent-before-child database locks; passkey physical storage convergence and checksum-bound prepared migration v5; bounded backup/restore/credential-rotation/monitoring paths; release, ruleset, environment, and secret-artifact governance; and a durable emergency-stop fence plus serialized cutover lock that prevents an in-flight legal operation from restarting the fixed production project after `down` succeeds.
- Verified: PostgreSQL 16 full Vitest matrix 56 files / 648 tests / 0 skips; typecheck and Next production build; full Swift 484 tests with 2 opt-in live skips under normal and strict-concurrency/warnings-as-errors, plus Thread Sanitizer with one additional intentional process skip and no race finding; operations 62/62, frontend recovery 6/6, credential rotation 19/19, systemd 2/2; governance 5/5 + 10/10; secret scanner 22/22 and 345 repository files clean; dependency audits reported zero vulnerabilities; real pinned Caddy config validated; shell syntax and diff whitespace clean. Rebuilt the current app/DMG as universal arm64+x86_64, verified the ad-hoc hardened-runtime signature, single-instance plist flag, and DMG checksum. Packaged UI QA covered unlock, Portfolio, Wallets, privacy-safe diagnostics, navigable table accessibility, share-safer default export, and the explicit identifying-export disclosure.
- Memory: updated `memory/bugfixes/2026-07-21-l6-data-and-release-boundaries.md` with the durable recovery, auth lock/capacity, storage-policy, privacy-output, secret-scan, and terminal-stop invariants; sync/doctor follows this entry.
- External gates: public release remains blocked on owner-controlled Developer ID signing/notarization, live production environment/DNS/deploy, GitHub release-environment reviewers, pager delivery, offsite recovery custody, and timed cross-host RTO evidence. The locally rebuilt artifact is intentionally ad-hoc signed and was not published.
- Next: sync/doctor semantic memory, commit and push `main`, require replacement GitHub CI fully green, then record the exact commit/run closure.

## 2026-07-21 15:20 +03 - Codex (hermetic service-control CI follow-up)

- Task: Followed commit `bd86def` through CI run `29829279036`; native normal/strict/TSan/release/universal packaging, deployment/Compose/Caddy, and secret-artifact hygiene passed, but the web job stopped in deployment fixtures before its intended assertions.
- Root cause: `deployment.test.ts` did not override the new production `ADDRESS_ATLAS_CONTROL_ROOT`, so a clean runner fell back to `/var/lib/address-atlas/service-control`; unlike the development Mac, it had no pre-existing `/var/lib/address-atlas` parent and correctly failed the production canonical/private-root gate with status 66.
- Changed: bind service-control state to each test's realpath-resolved 0700 temporary directory and assert that exact hermetic path and private parent mode. Production validation remains unchanged and fail-closed.
- Verified: focused deployment suite passed 59/59 in 112 seconds; an independent review confirmed the diagnosis and minimal secure fix.
- Memory: added `memory/gotchas/2026-07-21-hermetic-service-control-fixtures.md`.
- Next: sync/doctor memory, commit and push the follow-up, then require the replacement GitHub CI run to finish fully green.

## 2026-07-21 20:54 +03 - Codex (public GitHub polish)

- Task: Brought the public repository presentation and contribution surface up to the product's current quality bar without inventing a release, hosted service, or homepage.
- Changed: replaced the operator-heavy root README with a product-first story, exact 20-network coverage, explicit privacy/trust boundaries, source-first status, native-first setup, and Codex-as-engineering-collaborator disclosure; added `PRIVACY.md`, `CONTRIBUTING.md`, `CODE_OF_CONDUCT.md`, `SUPPORT.md`, structured bug/feature forms, and a trust-boundary PR template; added a deterministic 1280x640 social card and corrected stale export, release, and zero-knowledge wording in public docs.
- GitHub: set the repository description and 20 topics, added seven product-area/privacy/accessibility triage labels, uploaded the custom social preview, enabled private vulnerability reporting, disabled empty Wiki and Projects surfaces, and left Discussions and homepage empty until a real community need and public Devpost/product URL exist.
- Verified: issue-form YAML, secret-allowlist JSON, social-card SVG/XML, rendered PNG dimensions and visual output, all new local Markdown links, README secret-bootstrap logic, `git diff --check`, repository secret-artifact scan (357 files), secret-scanner self-tests (22/22), custom Open Graph state, live metadata/settings, and repo semantic-memory doctor (24/24).
- Memory: added `memory/decisions/2026-07-21-public-repository-positioning.md` and synced the local index.
- Next: commit and push `main`, require the replacement CI run to finish green, then add a real synthetic-data app screenshot and changelog with the first signed public release; set the homepage only when the Devpost or product URL is live.
