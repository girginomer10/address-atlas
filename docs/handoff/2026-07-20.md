# Handoff — 2026-07-20

## 2026-07-20 14:05 +03 - Claude

- Task: Ran a 6-agent runtime wiring & reachability audit (/deepreview) over the full tree (API layer, sync core lib, native sync/auth/crypto, native scanners/storage, test gaps, deployment wiring), then fixed every finding via 5 parallel fix agents. This also finalizes and ships the previously uncommitted remediation checkpointed in `2026-07-16.md` — today's audit re-traced that work in place (including the `postgres.ts` schema-contract/reconciliation slice flagged there as mid-edit) and found all chains complete and wired.
- Audit outcome: 1 P1 (operator broadcast `message` decoded by the Mac app but never rendered — full cross-stack no-op), ~14 P2s, plus risks. One subagent finding was refuted during the coordinator pass: the claimed X-Forwarded-For spoofing does not apply to `caddy:2-alpine` (Caddy ≥ 2.5 discards untrusted client XFF); it was downgraded to an explicitness fix.
- Fixes (highlights):
  - Native app now renders the operator broadcast message from `/config/native` as a banner (AppState publishes normalized `operatorMessage`; StatusLine renders it app-wide).
  - AppState gained network-boundary injection seams (`httpClient`, `passkeyAuthenticator`); new AppStateBehaviorTests drive the real upload/download/CAS-conflict/scan-merge glue with stubs only at HTTP.
  - New `GET /livez` (DB-free liveness, allowlisted in sync-only mode); Caddy active health probe moved to it so a Postgres blip no longer 502s every route; container healthcheck stays on deep `/healthz`. Caddy now sets `header_up X-Forwarded-For {client_ip}` explicitly; dead `x-real-ip` fallback removed from `clientKey`.
  - New `src/instrumentation.ts`: production boot fails fast on invalid sync/passkey/DB config instead of at first request.
  - Stale-version PUT now returns 409 even when daily quota is exhausted (409 takes precedence over 429; quota still charged); pinned by tests.
  - Stablecoin balances fall back to exactly $1.00 only when the live CoinGecko price is missing (live price always wins); warnings distinguish the fallback.
  - Dead surface removed: singular `rateLimit` export, `tokens.ts` re-exports, Swift `PasskeyOptionsResponse`/`AnyCodable`/`SendableValue`, non-throwing exchange `scan` wrapper, dead `bundled.exchanges` override entries (tripwire + type kept for wire compat; getters documented as deliberately hardcoded).
  - Test gaps closed: GET `/vault/latest` success path (unit + integration + non-circular Swift fixture); real PUT→GET round-trip and healthz through the real handlers against real Postgres (gated, runs in CI); passkey options route driven through the real lib with `expectedOrigin`/`expectedRPID`/challenge-binding assertions; `NativePasskeyBridge` ceremony extracted to `runPasskeyCeremony(input, deps)` with 7 node-env tests; Binance/Kraken signing known-answer vectors (independently derived); per-chain request-URL assertions for all 20 chains; server-shaped `/config/native` decode test + `configVersion == 5` cross-pins on both sides.
  - Config-version deploy-ordering invariant (server `NATIVE_ENDPOINT_CONFIG_VERSION` must be raised before shipping a client with a higher bundled version; client fails closed on both upload and download) documented in `server/sync/README.md` + `RELEASE_CHECKLIST.md` and pinned by tests. `npm audit --omit=dev` added to CI (currently 0 vulnerabilities). Dockerfile runner now copies `next.config.mjs`. Account deletion doc claim in `tokens.ts` corrected to out-of-band/admin operation. Cert-pinning absence documented as deliberate in `HTTPClient.swift` + native README.
- Verified: `npm run typecheck` clean; `npx vitest run` 217 passed / 17 skipped (DB-gated — CI provides Postgres); `npm run build` clean (`/livez` present, proxy registered); `swift test` 209 executed, 0 failures, 2 skipped (live-exchange smoke, credential-gated). Local machine has no Docker/Caddy/psql — compose/Caddy validation and real-Postgres integration tests are owned by CI; watch the next CI run to green.
- Memory: none added this session; `memory/` curated notes from 2026-07-14 are included in this commit unmodified.
- Next: watch GitHub CI (web + native + deployment-config jobs, including the newly un-skipped integration tests) to green. Remaining external gates unchanged: Developer ID/notary credentials and live exchange smoke tests belong to a credentialed release machine.

## 2026-07-20 14:15 +03 - Claude (CI follow-up)

- Task: The push above produced the first-ever real-Postgres CI execution of the hardened schema code, which failed all 11 postgres integration tests (note: main's CI was already red before this session — the prior commit failed on a since-fixed Swift compile error in `NativeScanner.swift`, so this was not a regression baseline). Reproduced locally on a disposable Postgres 16 (`brew install postgresql@16`, cluster in the session scratchpad) and fixed both root causes; pushed as `904dc9e`.
- Root causes: (1) `assertStorageBootstrapSurfaceSafe` demanded the exact constraint count on `sync_storage_usage` before any DDL, so a missing repairable check failed closed before `CORE_SCHEMA_REPAIR_SQL` could re-add it — the gate rejected schemas its own repair path exists to fix; it now tolerates missing contract constraints and still rejects unexpected/weaker constraints, triggers, and stray indexes. (2) The opaque-data test's raw `vault_snapshots` INSERT bypassed the global byte-counter charge, so the strict delete trigger raised during cleanup and the poisoned `afterEach` cascaded to every later test; the test now mirrors the production charge. Durable lesson recorded in `memory/bugfixes/2026-07-20-storage-bootstrap-ordering.md`.
- Verified: fresh-DB full vitest 234/234 (0 skipped) + second bootstrap over the existing schema + tsc clean locally; GitHub CI run 29737564223 fully green (Web and sync server, Native macOS package, Deployment configuration).
- Environment note: this Mac now has Homebrew `postgresql@16` available for future integration-test iteration (no running daemon left behind; the scratchpad cluster was session-local).
