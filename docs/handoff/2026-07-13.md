## 2026-07-13 12:38 +03 - Codex

- Task: Verified the Opus audit against the current implementation, fixed every confirmed actionable finding, and ran independent adversarial follow-up reviews across the server and native app.
- Changed: repaired Kraken standard-Base64 signing and process-wide monotonic nonces; added strict locale-aware numeric, EIP-55, Cosmos, RPC, response-size, redirect, retry, and provider-error handling; corrected scan warnings, totals, attribution, cancellation, sync persistence, passkey origin binding, and UI gates; hardened runtime/native config, envelope validation, passkey storage, quotas, aggregate accounting, cleanup, health probes, transaction cleanup, and rolling-safe phased Postgres schema migration; made release tooling fail closed and portable across Homebrew prefixes.
- Verified: `npm test` (79 passed, 1 credentialed Postgres integration test skipped), `npm run typecheck`, `npm run build`, production/full `npm audit` (0 vulnerabilities); `swift test` (101 passed, 2 credential-gated live exchange tests skipped), Thread Sanitizer (same result, no races), release build, universal `arm64` + `x86_64` app packaging, ad-hoc signature verification, shell syntax, Compose YAML parsing, and `git diff --check`.
- Memory: none; no repo semantic-memory system is installed. Durable lessons captured here are to validate EVM checksum before canonicalization, keep signed exchange routes binary-pinned, discard Postgres clients after ambiguous rollback/lock timeouts, and separate storage-counter and snapshot-trigger migration phases for rolling compatibility.
- Next: run the skipped Postgres migration/concurrency integration test and Docker/Caddy smoke test when Docker or Postgres is available; run live exchange tests with dedicated read-only credentials; configure Developer ID and notarization credentials for a public macOS release.

## 2026-07-13 14:05 +03 - Codex

- Task: Independently verified the six-agent Claude sync audit and fixed every confirmed actionable finding.
- Changed: repaired SimpleWebAuthn challenge binding; added real-Postgres passkey/vault invariant coverage and SHA-pinned CI; made macOS 14 date decoding tolerant of fractional seconds; wired bounded proactive native-config refresh with request coalescing; hardened Keychain first-run insertion, SQLite `0600` creation, passkey/browser privacy, no-store/CSP responses, exact-replay quota semantics, and native-config prototype-key validation. Documented that the reported XFF bypass is not present in the published Caddy topology because Caddy sanitizes incoming forwarded headers by default.
- Verified: temporary PostgreSQL 18.4 full suite (`107/107`); `npm run typecheck`; production `npm run build`; `npm audit` (0 vulnerabilities); native tests (`109` passed, 2 credential-gated live tests skipped); native release build; universal `x86_64` + `arm64` app packaging and signature verification; live production-page CSP header smoke test; Caddy 2.11.4 validation; Compose validation; actionlint; shell syntax; `git diff --check`.
- Memory: none; no repo semantic-memory system is installed. Durable lessons are to pass raw challenge bytes to SimpleWebAuthn and persist the exact returned browser challenge, keep Postgres invariants exercised against a real engine in CI, and treat Caddy as the forwarded-header trust boundary unless a constrained upstream proxy is explicitly introduced.
- Next: the first GitHub CI run passed on PostgreSQL 16, macOS, and the production Docker image build; only credential-gated live exchange tests remain for release QA.

## 2026-07-13 15:14 +03 - Codex

- Task: Fixed all seven confirmed findings from the second independent deep review.
- Changed: made replay and rejected-conflict bytes consume durable quota without logical writes; distinguished deleted sync accounts from empty vaults; aligned bounded native/server app-version parsing and made compatibility checks fail closed; validated remote snapshot metadata before checked version arithmetic; added revision/blob CAS plus a mixed-version write guard to prevent cross-process vault overwrites; bounded recovery imports to 64 KiB; and followed Cosmos bank/staking pagination with repeat-key, page-count, and standard-Base64 cursor-encoding guards.
- Verified: temporary real PostgreSQL full suite (`117/117`), `npm run typecheck`, production `npm run build`, `npm audit --omit=dev` (0 vulnerabilities), native tests (123 executed, 2 credential-gated live tests skipped, 0 failures), focused Cosmos pagination and bidirectional mixed-version CAS tests, universal `x86_64` + `arm64` app packaging, ad-hoc signature verification, and `git diff --check`.
- Memory: none; no repo semantic-memory system is installed. Durable lessons are to account for bytes on every authenticated upload outcome, treat remote metadata and compatibility policy as bounded wire formats, and pair whole-document persistence with a revision baseline.
- Next: only the existing credential-gated live exchange smoke tests and public Developer ID/notarization steps remain outside this remediation scope.

## 2026-07-13 17:18 +03 - Codex

- Task: Fixed all eight findings from the third deep review, then closed the sync-size and legacy-data issues found by two independent remediation audits.
- Changed: made encryption write/read size limits symmetric; enforced the exact 8 MB encoded sync-envelope budget with post-sync metadata headroom, oldest-first atomic history pruning, visible prune notices, and safe legacy unlock/recovery paths; refreshed in-memory persisted timestamps without changing the public `save(_:)` contract; priced USD stablecoins from live market data; unified `swift run` and bundle versions; committed wallet labels on edit completion; hardened CSV CR/CRLF escaping; shared ASCII CoinGecko-ID validation while retaining legacy tokens as unpriced; and fixed fragment-based TRON explorer URLs.
- Verified: temporary PostgreSQL full suite (`117/117`), `npm run typecheck`, production `npm run build`, `npm audit` (0 vulnerabilities); native tests (148 executed, 2 credential-gated live tests skipped, 0 failures), full Thread Sanitizer run (same result, no races), release builds, universal `x86_64` + `arm64` app packaging, source/bundle version `0.2.0`, ad-hoc signature verification, shell syntax, and `git diff --check`.
- Memory: none; no repo semantic-memory system is installed. Durable lessons are to budget the encoded sync envelope rather than plaintext, reserve `markSynced` metadata before persistence, keep oversized legacy vaults locally accessible for repair, and degrade invalid legacy pricing metadata without dropping valid on-chain assets.
- Next: only the existing credential-gated live exchange smoke tests and public Developer ID/notarization steps remain outside this remediation scope.

## 2026-07-13 19:18 +03 - Codex (paused; uncommitted WIP)

- Task: Started fixing all 16 findings from the fourth repo-wide deep review. Work was deliberately paused when the user said the computer would be shut down; all three review/fix subagents were interrupted cleanly.
- Baseline: branch `main`; `HEAD` and `origin/main` are both `9e305bffe89fc790ee7208b431091f3804db0971`. The working tree contains the entire remediation as uncommitted changes. Do not discard, reset, or overwrite it. No commit or push was made in this pass.
- Changed (server/ops, implementation complete): preserved explicit Postgres and Caddy production volumes and made ambiguous discovery fail closed; added versioned deployment management; made health checks validate the live schema; required exact domain/origin agreement for passkeys; separated pre-auth abuse quota from authenticated quota; bounded Postgres pool settings; removed remote Google font loading; added focused, deployment, rate-limit, health, config, and Postgres tests.
- Changed (native core, implementation complete): repaired Swift/Xcode numeric inference; corrected the Optimism USDT contract and Polygon RPC; retired Stargaze from new scanning while retaining saved legacy addresses; implemented wall-clock HTTP deadlines with transport cancellation; bounded native URL ports and handled IPv6 loopback; enforced shared UUID sync-account identifiers; bumped native endpoint config to v4; expanded scanner, sync, crypto, and endpoint tests.
- Changed (native app/release, landed but needs final full verification): bound new Kraken records to a per-installation device ID; made legacy, cross-device, and duplicate raw-key states fail closed; added a locked, atomic, permission-checked, bounded persistent Kraken nonce store; validated passkey callback IDs; cleared stale transient UI messages on navigation; made `CFBundleVersion` monotonic/configurable and shallow-clone-safe. The stale non-UUID `AppStateBehaviorTests` fixtures and the build-version empty/shallow cases were fixed immediately before pausing.
- Finding coverage: code now exists for all 16 items (Swift CI compile, production volumes, Optimism USDT, Polygon RPC, retired Stargaze, HTTP deadline, Kraken nonce/device safety, schema health, passkey domain invariant, pre-auth quota, account-ID validation, URL/port validation, bundle build number, DB resource controls, local fonts, and stale navigation messages). Findings 7, 13, and 16 still need the strongest end-to-end verification; the whole set still needs the planned residual miss-hunt.
- Repo memory: first-time local semantic-memory scaffolding is present in `AGENTS.md`, `memory/`, `scripts/repo-memory.py`, `src/lib/repo-memory.test.ts`, `.gitignore`, and `package.json`. Initial compile, init, doctor, search, and test checks passed, but the independent residual audit was interrupted and durable bugfix/gotcha notes have not yet been curated.
- Verified before the final interrupted edits: server focused tests `49/49`, deployment `10/10`, real rate-limit `2/2`, full server suite `131` passed with `11` environment-gated skips, typecheck/build/audit clean, shell/Compose checks clean; native-core focused tests `58/58`; native-config tests `25/25`; Kraken focused tests last reported `6/6`. The last full native run executed `157` tests with `2` credential-gated skips and only `3` stale UUID-fixture failures; those three fixtures are now changed but the full suite was not rerun. Current `git diff --check` passes.
- Not yet verified: post-interruption full Swift suite and release build; build-version shell regression test; Thread Sanitizer; universal bundle/signature; packaged-app UI smoke; Docker/Caddy and real-Postgres drift integration; credential-gated live exchange tests; final memory doctor/audit; second independent residual review; complete npm/typecheck/build/audit rerun over the final combined tree.
- Resume: first inspect `git status --short --branch` and the uncommitted diff. Then run the build-version regression script and full Swift suite, finish Kraken/UI/release QA, run the complete server/native verification matrix, finish the semantic-memory audit and curated notes, perform a second miss-hunt, update this handoff, and only then commit all intended files and push `main` while monitoring CI. Keep generated artifacts, credentials, environment files, and local memory indexes out of git.
