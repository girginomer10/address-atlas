## 2026-05-08 00:08 CEST - Codex

- Task: Continued native Mac scanner and credential-vault parity.
- Changed: Added built-in Solana SPL registry entries, custom token registry merging for EVM/Solana, EVM ERC-20 `balanceOf` probing, Solana SPL Token/Token-2022 account parsing/scanning, parser/registry tests, and an `ExchangeCredentialVault` helper that uses the dedicated exchange credential subkey.
- Verified: `swiftc -parse` over native Swift files, `npx tsc --noEmit`, `npm test`, `npm run build`, `git diff --check`, and conflict-marker scan.
- Not verified: `swift test` still cannot run in this workspace because SwiftPM fails linking the manifest under Command Line Tools before source compilation. Full Xcode verification is still needed.
- Memory: none; this extends the native scanner/credential implementation already documented in `docs/DEVELOPMENT.md`.
- Next: Verify/fix Swift compile under full Xcode, add native UI for custom tokens and exchange connections, implement Cosmos staking/rewards parity, and wire live exchange balance clients behind mocked signing tests.
