## 2026-05-09 18:39 CEST - Codex

- Task: Fixed deep review findings around stale sync uploads, native RPC error handling, exchange failure isolation, and unsafe exchange endpoint overrides.
- Changed: Updated vault latest upload conflict handling, Mac sync preflight checks, native Solana/XRP scanner error paths, base58 address detection precedence, exchange snapshot error isolation, HTTPS-only exchange endpoint config, local duplicate cleanup, and regression tests.
- Verified: `npm test`, `npm run typecheck`, `npm run native:test`, `npm run build`, `git diff --check`, and conflict-marker scan.
- Memory: none; no repo semantic memory directory is installed. Durable gotcha covered by native regression test: TRON/XRP base58 detection must run before Solana.
- Next: Live exchange smoke tests remain opt-in via `ADDRESS_ATLAS_LIVE_EXCHANGE_TESTS=1`; no open blocker from this pass.

## 2026-05-09 18:56 CEST - Codex

- Task: Expanded supported networks and built-in coin/token coverage.
- Changed: Added Gnosis, Linea, Mantle, Scroll, and ZKsync Era to the web and Mac EVM registries; added common ERC-20 buckets for those networks; added CRV on Ethereum; added PYTH, RAY, mSOL, and ORCA to Solana SPL tracking; expanded exchange CoinGecko symbol mappings; updated native endpoint defaults, docs, and registry tests.
- Verified: `npm test`, `npm run typecheck`, `npm run native:test`, `npm run build`, `git diff --check`, and conflict-marker scan.
- Memory: none; no repo semantic memory directory is installed. Durable gotcha covered by tests: web and native chain registries must stay in lockstep for built-in network/token support.
- Next: Consider multicall/batching before adding many more EVM tokens, because each extra built-in token adds balance probes for every matching 0x wallet scan.
