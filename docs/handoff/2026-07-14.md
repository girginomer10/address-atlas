## 2026-07-14 14:13 +03 - Codex (paused; uncommitted WIP)

- Task: Resumed the fourth repo-wide deep-review remediation, started three independent closeout audits (native, server/ops, and repo memory), then stopped immediately at the user's request before shutdown.
- Stop state: all three subagents were interrupted cleanly. Branch `main` remains at `9e305bffe89fc790ee7208b431091f3804db0971`, identical to `origin/main`. The full remediation is still uncommitted in the working tree; do not reset, discard, or overwrite it. No commit or push was made.
- Changed since the 2026-07-13 pause: production volume discovery was narrowed to the expected Compose projects and exact legacy names so unrelated labeled volumes cannot be adopted; the deployment wrapper gained a safe preflight/config path; Caddy and the app gained bounded request-header/body upload deadlines with cancellation and a `408` path; schema health now checks the exact enabled trigger shape and initialization repairs drift. CI, release-doctor, and release/development docs now use the deployment wrapper and exercise the build-number regression path.
- Native closeout: the audit was interrupted before a final report. Re-check the timing of navigation-message clearing, run a real shallow-clone build-number regression, and verify that foreign-device Kraken records are skipped without cross-device status ping-pong. Preserve the existing `native/AddressAtlasMac/README.md` build-number documentation.
- Repo memory closeout: scaffolding and tests remain in the tree, but the independent audit and final curated bugfix/gotcha notes were interrupted. Inspect current `memory/` changes before adding anything, then run sync and doctor.
- Verified in this slice: `git diff --check` is clean; official/current sources and live RPC probes confirmed the Optimism USDT contract, Polygon chain ID/RPC behavior, and retired Stargaze status. Partial focused server tests were reported passing while the audit was running, but no interrupted subagent produced a final signed-off report, so rerun the complete matrix. Docker, Caddy container validation, and a local real-Postgres run were unavailable on this machine.
- Resume: begin with `git status --short --branch` and inspect the whole diff plus this note. Finish the native/server/memory closeout audits and second miss-hunt; run full npm tests, typecheck, build, audits, Swift tests, Thread Sanitizer, release/universal packaging, build-version regressions, shell/config checks, memory sync/doctor, and packaged-app UI smoke. Then add the final handoff and durable memory notes, exclude generated/local/credential artifacts, commit the intended changes, push `main`, and monitor CI to green.

## 2026-07-14 14:24 +03 - Codex (paused again for shutdown)

- Task: Continued the exhaustive remediation closeout, then stopped all active work at the user's request so the computer can be shut down safely.
- Stop state: native, server/ops, and repo-memory reviewers are all interrupted. Branch `main` is still at `9e305bffe89fc790ee7208b431091f3804db0971`; all remediation remains present as uncommitted working-tree changes. Do not reset, clean, stash over, or overwrite this tree. No commit or push was made.
- Newly completed in the working tree: strict cross-runtime date validation now rejects normalized impossible timestamps while retaining the intended year-zero/fractional-date compatibility; Kraken raw-key duplicates fail closed even when records mix bound, legacy, or malformed device bindings; the Swift decoder no longer captures non-Sendable date formatters; native slow-response timeout coverage was made CI-tolerant while still proving real cancellation; the stale schema-v1 fixture was corrected.
- Verified before this pause: the server closeout reported all 49 owned files reviewed, `npm test` at 20 passed files plus 2 integration skips (161 passed tests plus 11 skips), typecheck/build/audits/focused tests clean, and shell/YAML/diff checks clean. Treat this as interim evidence only: native and memory closeouts and the whole-diff miss-hunt were interrupted, so rerun the complete matrix from the final combined tree.
- Open item to inspect first: `server/sync/manage-prod.sh` may return early when an explicitly configured volume name is missing, before unscoped logical-label discovery. Confirm whether a typo can create a fresh volume beside an existing custom-project data volume; if so, make the preflight fail closed and add regression coverage.
- Resume order: (1) inspect status/diff and this note, (2) finish the three closeout reviews and the 16-finding evidence ledger, (3) resolve the deployment-wrapper edge case, (4) run full npm/Swift/ThreadSanitizer/release/package/shell/memory verification, (5) smoke-test the packaged macOS app, (6) perform the final secrets/generated-artifact and second miss-hunt sweep, (7) update handoff and curated memory, then commit, push `origin/main`, and monitor GitHub CI until green.
- Memory: no semantic note was added for this pause because it contains session state rather than a durable technical decision; the durable date/volume lessons already queued in the working tree still need final review, sync, and doctor.

## 2026-07-14 14:40 +03 - Codex (authoritative shutdown checkpoint)

- Task: Stopped the exhaustive deep-review/remediation closeout at the user's request. The whole-diff reviewer and its nested native cross-check were interrupted cleanly; no background reviewer should still be running.
- Stop state: branch `main` remains at `9e305bffe89fc790ee7208b431091f3804db0971`, still identical to `origin/main`. The complete remediation and this handoff are uncommitted working-tree changes. Do not reset, clean, discard, overwrite, or start from a fresh checkout. No commit or push was made.
- Completed independent reviews: native covered 36 files and reported 168 tests passing with only 2 credential-gated live-exchange skips, the same result under Thread Sanitizer, strict-concurrency/warnings-as-errors and release builds passing, and the shallow/full-history build-number regression passing at build 50. Server/ops reported the full npm suite at 166 passed plus 11 intentional integration skips, with focused tests, typecheck, production build, audits, shell/YAML/diff checks clean. Repo-memory covered 22 areas, passed its 8 focused tests, recovered corrupt SQLite/sidecars, rejected main-DB symlink/hardlink targets, and reported sync/doctor healthy at 5/5 notes.
- Root verification on the combined tree: `npm test` passed at 166 plus 11 skips; typecheck, Next production build, and both npm audits passed. The universal macOS package built for arm64+x86_64 with bundle/source version `0.2.0`, build `50`, and valid ad-hoc code signature. After clearing a stale generated SwiftPM test-bundle signature with `swift package clean`, the clean native suite passed at 168 tests with the same 2 credential-gated skips. The packaged app launched and rendered the encrypted-vault unlock screen; unlocked navigation could not be exercised because the existing Keychain item invoked macOS SecurityAgent, which was deliberately not bypassed or modified.
- Three unresolved candidates from the interrupted final miss-hunt must be the first work on resume: (1) schema readiness validates names/trigger/singleton but not all PK/unique/type/default/function-body invariants, so drift such as removing `vault_snapshots(user_id)` uniqueness may leave `/healthz` green while `ON CONFLICT(user_id)` fails; harden and test every runtime-required invariant. (2) Kraken `deviceIdentifier`, HMAC key, and nonce map currently live together in ordinary Application Support JSON, so copying that file to another Mac can clone the supposed device binding and permit independent conflicting nonce streams; move the binding secret/identity into a non-clonable or separately protected store and add cross-install tests. (3) native accepts `http://127.0.0.1` and `http://[::1]`, but server passkey configuration only permits local HTTP for exact `localhost` and rejects IP RP IDs; align URL acceptance and authentication behavior, then add end-to-end config tests.
- Remaining closeout after those fixes: finish the interrupted second miss-hunt and the explicit 16-finding evidence ledger; rerun the full npm/Swift/Thread Sanitizer/release/package/static matrix; run final secret/generated-artifact scans, memory sync/doctor, and release-doctor; append the completion handoff and curated durable lessons; stage only intended files, commit, push `origin/main`, and watch GitHub CI (including real PostgreSQL, Caddy, and container checks) until green.
- External gates, not code completion: no local Docker/Caddy/psql, no Developer ID/notary credentials, no live exchange credentials, and no safe automated approval of the user's Keychain prompt. CI and a credentialed release machine must cover those gates.
- Memory: no new semantic note was added for this shutdown checkpoint because it records resumable session state, not a finalized durable decision.

## 2026-07-14 14:50 +03 - Codex (latest shutdown checkpoint)

- Task: Stopped again immediately at the user's request before computer shutdown. The schema-contract and live chain-registry reviewers were interrupted; the Kraken reviewer had completed. No background reviewer remains active.
- Stop state: branch `main` is still based on `9e305bffe89fc790ee7208b431091f3804db0971`. The entire remediation remains as uncommitted working-tree changes (currently 55 tracked files changed plus the existing untracked repo/memory/test/ops files). Do not reset, clean, discard, overwrite, or switch to a fresh checkout. No commit or push was made because the combined tree is unfinished and has not received final verification.
- Newly completed and verified: Kraken nonce state is now v2 with a separate 32-byte `WhenUnlockedThisDeviceOnly` Keychain binding secret; copied state, missing/different secrets, Keychain failures, v1 rotation, and case-folded exchange-asset alias collisions are covered. The reviewer reported 17/17 focused Kraken tests, the full Swift suite at 172 passed plus 2 credential-gated skips, focused Thread Sanitizer, strict-concurrency/warnings-as-errors, release build, and diff check all clean.
- Newly completed by root: best-effort consumed-passkey-challenge pruning is now rate-limited and single-flight so slow database cleanup cannot accumulate unbounded pool waiters; its focused server tests and typecheck passed. Native/server loopback policy was aligned to permit local plain HTTP only for exact `localhost`, reject IPv4/IPv6 and legacy IPv4 spellings, and require WebAuthn-compatible DNS host syntax. The final DNS-syntax additions were made immediately before this pause and still need focused/full reruns.
- Interrupted partial work: `src/lib/sync/postgres.ts`, `postgres.test.ts`, and `postgres.integration.test.ts` contain the schema reviewer’s in-progress contract/readiness/repair hardening. Treat those edits as unsignalled WIP: inspect the diff first, complete or correct it, and rerun focused tests against both mocked and real PostgreSQL CI. The live all-chain endpoint/token audit was interrupted before a report and must be restarted.
- First open bug on resume: Coinbase account pagination currently lacks the documented account UUID in its model, so a repeated account at a page boundary may be counted twice. Add UUID-based de-duplication and a cross-page regression test after inspecting the finished Kraken changes in the same Swift files.
- Resume order: (1) inspect status and the full diff, especially interrupted PostgreSQL work; (2) run the focused native URL tests; (3) fix Coinbase UUID pagination de-duplication; (4) restart the live chain/token audit and finish the second miss-hunt plus explicit finding ledger; (5) rerun the complete npm, Swift, Thread Sanitizer, release/universal packaging, static, memory, and packaged-app smoke matrix; (6) update the stale Kraken semantic-memory note and add only finalized durable lessons; (7) append completion handoff, stage intended files only, commit, push `origin/main`, and monitor CI to green.
- External gates remain unchanged: no local Docker/Caddy/psql, no Developer ID/notary credentials, no live exchange credentials, and no safe automated approval of the user's Keychain prompt. CI and a credentialed release machine cover those gates.

## 2026-07-14 14:51 +03 - Codex (authoritative shutdown checkpoint)

- Task: Paused the exhaustive deep-review remediation immediately for computer shutdown and wrote this durable resume point. The active schema-contract, Kraken final-review, and live chain-registry reviewers were all interrupted; `collaboration.list_agents` confirmed all three as `interrupted` and no background reviewer remains running.
- Repository state: branch `main`, local HEAD `9e305bffe89fc790ee7208b431091f3804db0971`, still equal to `origin/main`. The working tree currently contains 55 modified tracked paths and 9 untracked paths. Nothing from this remediation has been committed, pushed, reset, cleaned, or stashed. Preserve the working tree exactly; do not overwrite it with a fresh checkout.
- Treat current agent-owned edits as unfinished WIP: `src/lib/sync/postgres.ts` plus its unit/integration tests contain the interrupted schema-contract hardening; `ExchangeClients.swift` and `ScannerHardeningTests.swift` may contain mid-review Kraken/Coinbase changes made after the 14:50 checkpoint. Review their complete diffs before editing or trusting earlier test totals.
- Kraken/Coinbase resume checklist: verify safe recovery when v2 state exists but the device-only Keychain secret is genuinely missing (while failing closed on Keychain errors); prevent case-folded alias-map traps and all duplicate normalized raw-key groups; add stable Coinbase account-UUID de-duplication across repeated cursor pages; bound and validate provider-controlled exchange symbols. Rerun focused and full Swift tests after inspection.
- Schema resume checklist: audit the expanded readiness/repair implementation for every runtime-required primary-key, unique, foreign-key, type, default, check, trigger, and function-body invariant; simplify or correct it where necessary; cover drift with mocked tests and real-PostgreSQL CI. No final reviewer report was produced before interruption.
- Registry and URL resume checklist: restart the read-only live validation of every configured chain/token/RPC endpoint because it ended without a report. The exact-`localhost` HTTP/WebAuthn alignment and IP-literal rejection are present in the tree but require focused and full reruns from the combined final state.
- Root-only change just before this stop: `.gitignore` now also excludes common private-key/package and local credential files (`*.key`, `*.p12`, `*.pfx`, `.netrc`, `.npmrc`, and `.yarnrc*`). Review it with the rest of the intended diff.
- Verification status: no new full verification was run after the latest interrupted schema and exchange edits. Earlier passing totals in this file are historical evidence only, not sign-off for the current combined tree. On resume, finish the miss-hunt and 16-finding ledger, then rerun the entire npm, Swift, Thread Sanitizer, strict/release/universal-package, deployment/static, secrets/generated-artifact, memory sync/doctor, release-doctor, and packaged-app smoke matrix.
- Completion path: after all reviews and verification pass, append a final handoff, update only finalized semantic-memory lessons, stage intended files, inspect the staged diff for credentials/generated artifacts, commit to `main`, push `origin/main`, and monitor GitHub CI (including PostgreSQL/container checks) until green. External release gates remain Developer ID/notary credentials, live exchange credentials, and the user's Keychain approval.

## 2026-07-14 15:05 +03 - Codex (latest authoritative shutdown checkpoint)

- Task: Stopped the exhaustive deep-review remediation immediately for computer shutdown. The live chain-registry audit and PostgreSQL adversarial review were interrupted; the Coinbase reviewer had completed. `collaboration.list_agents` confirmed that no subagent remains running.
- Repository state: branch `main`, local HEAD `9e305bffe89fc790ee7208b431091f3804db0971`. The unfinished combined working tree contains 58 modified tracked paths and 9 untracked paths. Nothing in this remediation has been committed, pushed, reset, cleaned, or stashed. Preserve this exact working tree; do not replace it with a fresh checkout.
- Completed since the 14:51 checkpoint: Coinbase account pages now decode canonical UUIDs, de-duplicate repeated accounts across cursor pages, and skip malformed/missing/non-string account IDs with an aggregate warning. The reviewer reported 19/19 focused tests, 174 Swift tests passing plus 2 credential-gated skips, and a clean strict-concurrency build. Root also corrected the built-in Solana BONK and WIF mint addresses/decimals and passed the focused registry reference-token test.
- PostgreSQL state: the expanded schema contract/readiness/repair implementation is now present in `src/lib/sync/postgres.ts` and its unit/integration tests. The implementation review reported the full npm suite at 175 passed plus 11 skips; root reran the focused PostgreSQL and health tests at 16 passed plus 8 intentional integration skips. The independent adversarial review was interrupted, so first decide whether rejecting every harmless additive column/index/constraint/trigger creates false-red readiness before accepting the contract. Real PostgreSQL 16 validation remains CI-owned because no local PostgreSQL is available.
- Live registry audit, verified partial results: all 79 configured ERC-20 contracts returned deployed code and expected decimals. The Ethereum Llama RPC hostname no longer resolves; `https://ethereum-rpc.publicnode.com` was verified live with chain ID 1 and should replace it across the Swift registry, bundled native endpoint payload, server defaults, tests, docs, and a native config-version bump from 4 to 5. The existing Arbitrum and Polygon USDT addresses have upgraded in place to USDT0; retain addresses/decimals but change user-facing symbol/name and CoinGecko ID to `USDT0`/`USDT0`/`usdt0`, including stablecoin normalization. The non-EVM/Cosmos portion of the audit was interrupted and still needs completion.
- Changes deliberately not started before shutdown: the dead Ethereum endpoint replacement, native configuration v5 propagation, and USDT0 metadata updates. Apply them only after inspecting all current diffs. Then run focused native-config/deployment/registry tests.
- Verification status: focused schema, Coinbase, URL, and registry tests have passing evidence, but no final full matrix was run on the present combined tree. Earlier full-suite totals are historical evidence only. The final run still needs npm tests/typecheck/build/audits, clean Swift tests, Thread Sanitizer, strict/release/universal packaging, build-version regression, shell/YAML/static/secrets/generated-artifact checks, repo-memory sync/doctor, release-doctor, and packaged-app smoke testing.
- Resume order: (1) inspect `git status`, this checkpoint, and the complete diff; (2) finish the PostgreSQL false-positive/false-negative review; (3) apply the verified Ethereum/config-v5/USDT0 corrections and complete the non-EVM registry audit; (4) finish the second integration miss-hunt and explicit finding ledger; (5) run the full verification matrix; (6) update finalized semantic-memory notes, especially the now-stale Kraken binding and chain-registry notes, then sync/doctor; (7) append the completion handoff, stage intended files only, inspect for credentials/generated artifacts, commit to `main`, push `origin/main`, and monitor GitHub CI to green.

## 2026-07-14 15:10 +03 - Codex (final shutdown checkpoint)

- Task: Paused immediately for computer shutdown and captured the three reviewers' final checkpoints. `collaboration.list_agents` confirms the Kraken/exchange, PostgreSQL schema-contract, and live registry reviewers are all completed; no subagent remains running.
- Repository state: branch `main`; local HEAD and `origin/main` are both `9e305bffe89fc790ee7208b431091f3804db0971`. The unfinished combined working tree has 58 modified tracked paths and 9 untracked paths. Nothing from this remediation has been committed, pushed, reset, cleaned, or stashed. Preserve this working tree exactly and resume in this checkout.
- Kraken/exchange final report: v2 identity recovery/rotation, device-only Keychain binding, fail-closed Keychain and filesystem behavior, duplicate raw-key rejection, case-fold alias collision handling, canonical Coinbase UUID de-duplication/conflict removal, bounded provider symbols, and USDT0 price normalization are present. Focused Kraken/Coinbase tests passed 26/26, fiat/USDT0 tests passed 5/5, the full Swift suite passed 183 tests with 2 credential-gated skips, and strict concurrency plus warnings-as-errors passed. Final Thread Sanitizer must still be rerun on the settled combined tree.
- PostgreSQL final checkpoint: `src/lib/sync/postgres.ts` and its unit/integration tests now validate and safely repair the runtime-required table/column/default/nullability/key/FK/check/index/trigger/function contract, including non-deferrable conflict targets, enabled internal FK triggers, and WebAuthn counter range `0...4294967295`. Before the last counter/FK-trigger additions, 11 focused tests and typecheck passed; PostgreSQL-WASM then confirmed clean/repair cases and rejection of deferrable uniqueness and disabled FK triggers. Rerun focused tests/typecheck/diff checks and let CI execute the expanded real-PostgreSQL suite.
- Live registry final report: current tree has no further confirmed registry correctness mismatch. Live checks passed for 12/12 EVM chain IDs, 79/79 ERC-20 code/decimals, 10/10 Solana mints/decimals/CoinGecko mappings, TRON USDT, XRP, Bitcoin, Cosmos Hub, Osmosis, Celestia, and Stride. ChainRegistry/native/server endpoint sets match 20/20; 46/46 CoinGecko IDs returned prices. Confirmed fixes already in the tree are real BONK/WIF mints, Arbitrum/Polygon USDT0 metadata, and Ethereum migration from failing LlamaRPC to live `https://eth.drpc.org`; native config payload/version is now 5.
- Resume order: (1) read this entry and inspect the full diff, especially schema and exchange changes; (2) run focused schema/native-config/callback/exchange tests; (3) finish the root second miss-hunt and explicit 16-finding evidence ledger; (4) rerun the complete npm, Swift, Thread Sanitizer, strict/release/universal-package, build-version, shell/YAML/static/secrets/generated-artifact, memory sync/doctor, release-doctor, and packaged-app smoke matrix; (5) update only finalized curated memory notes and append the completion handoff; (6) stage only intended files, inspect the staged tree, commit to `main`, push `origin/main`, and watch GitHub CI to green.
- External gates remain: no local Docker/Caddy/psql, no Developer ID/notary credentials, no live exchange credentials, and no safe automated approval of the user's Keychain prompt. These are verification/release-environment gates, not permission to weaken the checks.

## 2026-07-14 15:19 +03 - Codex (authoritative shutdown checkpoint)

- Task: Resumed final deep-review closeout, then stopped immediately when the user said the computer would be shut down. The server, native, and ops/memory second-pass reviewers were interrupted; `collaboration.list_agents` confirmed all three are stopped and no subagent remains running.
- Repository state: branch `main`; local HEAD and `origin/main` remain `9e305bffe89fc790ee7208b431091f3804db0971`. The unfinished working tree still has 58 modified tracked paths and 9 untracked paths. No reset, clean, stash, commit, or push occurred. Preserve this exact checkout.
- Fresh combined-tree evidence: `npm test` passed 181 tests with 11 real-PostgreSQL skips; `npm run typecheck`, `npm run build`, `npm audit`, and `npm audit --omit=dev` passed. A clean `swift package clean && swift test` passed 185 tests with 2 credential-gated live-exchange skips. PublicNode and dRPC both returned Ethereum chain ID 1 in fresh probes. The current code/config/tests consistently use the official PublicNode root `https://ethereum-rpc.publicnode.com`; the earlier handoff claim that the final tree uses dRPC is stale and must be corrected, not propagated to code.
- Confirmed but not yet fixed native findings: (1) `server/sync/.env.production.example` and `server/sync/README.md` recommend `https://ethereum-rpc.publicnode.com/rpc`, but that path returned HTTP 404 while the root endpoint works; change the examples and add/adjust regression coverage. (2) After accepting remote endpoint config v7, `AppState.refreshEndpointConfig` can accept stale v6 from the same server and silently downgrade endpoints/minimum-version policy; preserve monotonic accepted config versions per server and test rollback plus server-switch behavior. (3) `ZeroKnowledgeSyncClient.error` can surface an untrusted response `error` string up to the full response cap into SwiftUI without control/secret/length sanitization; reuse bounded provider-style sanitization and test it.
- Confirmed but not yet fixed ops/docs findings: (1) `release-doctor.sh` accepts misspelled/extra arguments, so `--strcit` silently disables strict notary validation; reject every invocation except zero args or exactly `--strict`. (2) The doctor can pass when the selected signing identity is an Apple Development identity merely because a different Developer ID identity exists; require the selected identity itself to match a `Developer ID Application` record. (3) `server/sync/README.md` claims to list the only sync routes but omits required `GET /auth/native`. (4) deployment tests set `ADDRESS_ATLAS_PROD_ENV_FILE` to an empty string, which falls back to a maintainer's ignored real `.env.production`; point tests at an explicit missing temporary path. Ops/memory focused tests were 26/26, build-number checks, shell syntax, diff check, and memory doctor 5/5 were clean before these findings.
- Root candidate to verify on resume: the server request-body absolute deadline uses `Date.now()` across streamed reads; a backward wall-clock adjustment can extend a slow-drip request despite the claimed absolute deadline. Decide with a monotonic-clock regression and replace with a monotonic deadline if confirmed. Also finish the interrupted server reviewer, including the PostgreSQL readiness false-positive/false-negative audit (especially harmless additive columns versus write-affecting drift).
- Resume order: (1) read this checkpoint and inspect the untouched current diff; (2) implement the confirmed PublicNode-example, config rollback, sync-error sanitization, release-doctor, route-doc, and hermetic-test fixes with regressions; (3) resolve the monotonic request-deadline and schema strictness candidates; (4) restart the three read-only second passes and complete the 16-finding ledger; (5) rerun npm/Swift/Thread Sanitizer/strict/release/universal/package/static/secrets/memory/release-doctor/UI checks; (6) update curated memory and final handoff, stage only intended files, commit/push `origin/main`, then watch CI to green.
