import AddressAtlasCore
import SwiftUI

// Placeholder; replaced by the real screen implementation.
struct ICloudScreen: View {
  @EnvironmentObject private var state: AppState

  var body: some View {
    IOSPage(title: "iCloud") {
      Surface {
        Text("iCloud is being built.")
      }
    }
  }
}
