DATABASE_URL="file:./.data/address-atlas.db"

# Native Mac encrypted sync server. The existing web app can keep using SQLite
# through DATABASE_URL; encrypted sync requires a separate Postgres URL.
SYNC_DATABASE_URL="postgres://address_atlas:address_atlas@localhost:15432/address_atlas_sync"
SYNC_SESSION_SECRET="replace-with-a-long-random-secret"
PASSKEY_RP_ID="localhost"
PASSKEY_RP_NAME="Address Atlas"
PASSKEY_ORIGIN="http://localhost:3000"
